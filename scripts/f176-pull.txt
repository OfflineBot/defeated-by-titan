# f176-pull — docs/NEXT.md §3D: THE ROPE PULLS FROM EVERY KEY, AND THE LEGS CANNOT WALK OUT OF IT.
#
# 🔴 **STILL RED AT ACT 2, ON PURPOSE, AND THE REASON CHANGED ON 2026-08-27 — READ THIS FIRST.**
#
# It encodes the user's own words below as asserts. TWO attempts at the feature were built and
# BOTH were refuted 2/2 by independent adversaries and reverted (`docs/FINDINGS.md` FIND-186).
# Attempt 3 — `Q-058`, a real `DistanceJoint` on a `Drive` rope — landed on 2026-08-27.
#
# ## What attempt 3 changed here, measured [offlinebot], debug build
#
# | assert | HEAD (no joint) | with the joint |
# |---|---|---|
# | ACT 2  `Speed < 2` | 6.000 ❌ | **7.630 ❌ — and it now means the OPPOSITE** |
# | ACT 2b `Height < 1` | 0.000 ✅ | 17.999 ❌ → **re-derived below** |
# | ACT 2b `Speed < 7`  | 0.000 ✅ | 49.313 ❌ → **re-derived below** |
# | ACT 2b `Speed > 3`  | 0.000 ❌ | **49.313 ✅** |
#
# The run is **1 of 9** red today, and the one red line is ACT 2's, deliberately.
#
# 🔴 **ACT 2's `Speed < 2` is the one that must NOT be quietly repaired, and it is left alone.**
# It encodes *"on the ground, hooked, holding `S`, the player does not move"*. But
# `docs/NEXT.md` §3D R1 does not say that — it says **"the pull is unconditional; `S` is the one
# input that is allowed to fight it, and even then it may only SLOW the approach, not reverse it
# into a retreat."** With the joint plus `FIND-172`'s always-on pull, one second of `S` from a
# taut 36.72 m rope takes the player to **35.637 m** — he is still closing — while the same
# second with the rope let go takes him to **39.619 m**. The assert reads 7.63 m/s and cannot
# tell those two apart: **`assert speed` cannot tell an escape at 6 m/s from a haul at 12 m/s**
# (`CLAUDE.md` §6 rule 5, second half), and the script harness has no distance metric to ask
# with — `rope` is the anchored ARM COUNT (`src/debug/script.rs`), not a length.
# **The measurement that CAN tell them apart is
# `tests/vector_rope.rs::f176_under_drive_walking_backwards_on_a_taut_rope_still_closes_on_the_anchor`,
# and it is green.** Whether this line should become a band around 12 m/s, or go, is the main
# head's call and the user's — `docs/QUESTIONS.md` Q-061.
#
# ✅ **ACT 2b's two asserts WERE re-derived, 2026-08-27, and that is not a loosening.** The
# header they replace said so itself: *"they were written against the reverted build to catch
# its ground elevator … ACT 2b is currently measuring an accumulated state, not the property it
# names. Whoever builds attempt 3 should re-derive it from a clean stance."* The clean stance is
# `tests/vector_rope.rs::probe_ground_walk_on_a_rope`. What it shows is that `height < 1` was
# never a property of this stance at all: the anchor is **34 m above and 14 m behind**, the rope
# is born **taut at 36.72 m**, and `A` is therefore a movement on a sphere whose centre is over
# the player's head. **Going sideways on that sphere goes UP.** That is §3F verbatim — *„zur
# seite gehen … aber NICHT das seil verlängern"* — and a 1 m ceiling forbids the feature.
#
#
# The user, 2026-08-26, verbatim:
#
#   „ok wichtig: wenn ich von seil weg gehe. also seil ist vorne und ich laufe zurueck werde
#    cih nicht ran gezogen. sonst werde ich ranzeogen! wenn ich a oder d druecke zur seite soll
#    ich auch noch rangezogen werden! AUCH. aber weniger! aktuell kann ich a druecekn und w und
#    ich gehe in einer geraden linie weg von dem ankerpunkt (nicht gut). wenn das seil shcon
#    eingezogen wurde soll es erstmal nicht laenger werden!"
#
#   ./target/debug/defeated_by_titan --headless --script scripts/f176-pull.txt --ticks 900
#
# and, as a SEPARATE run, the one that returns a usable exit code:
#
#   ./target/debug/defeated_by_titan --headless --script scripts/f176-pull.txt --ticks 900 >/dev/null 2>&1; echo $?
#
# ## What this run can measure and what it cannot
#
# ⚠️ **The script harness has no anchor-distance metric** (`speed`, `height`, `gas`, `titans`,
# `tick`, `health`, `kills`, `phase`, `rope`, `blades`, `sharpness` — `src/debug/script.rs`;
# `rope` is the anchored ARM COUNT, not a length), and §3D's and §3F's acceptance IS a distance.
# **So the matrix is a test, not a run:**
# `tests/vector_rope.rs::f3f_two_anchors_and_no_arm_ever_gets_further_from_its_own_anchor_than_its_rope_allows`
# and its `..._standing_on_the_ground` sibling — 288 cells each (4 separations × 2 elevations ×
# 4 yaws × 9 key combos), worst excess **0.0050 m** against **+51.1978 m** before the joint.
#
# What THIS run measures is the half a SPEED can see:
#
#   **R1 · on the ground, hooked, holding `S`, the player is not carried away from his anchor.**
#
# Before 2026-08-26 he walked out of his own rope at `player.run_speed_m_s` = 6 m/s, because
# `air_control`'s always-on pull is gated on `in_flight` and a hooked player standing up is
# `Grounded` (`player::integrator::movement_state`). Since `Q-058` the **joint** is what stops
# the walk: the rope is born taut at 36.72 m and `limits = (0, L)` corrects the moment the
# distance would exceed it.
#
# 🔴 **THE CONTROL, and without it ACT 2's number is worthless** — a player who is simply stuck
# on a wall also reads 0 m/s. ACT 3 lets go of the hook and holds the SAME key: it has to come
# back at the full run speed. One run, both numbers, no second binary.
#
# Measured 2026-08-27 [offlinebot], debug build, as the DISTANCE the speed is a proxy for
# (`tests/vector_rope.rs::f176_under_drive_walking_backwards_on_a_taut_rope_still_closes_on_the_anchor`):
#
#   one second of S from a taut 36.723 m rope     after
#   Drive, hooked                                 35.637 m   <- still closing (R1)
#   Pendulum, hooked                              36.715 m   <- stands; nothing pulls
#   Drive, rope let go                            39.619 m   <- a man walking away (the control)

wait 1.5                # world, camera, meshes and the render pipelines come up (90 ticks)

# ---- ACT 1 · get him onto his legs with a rope in front of him ------------------------------
# The same clear ground `scripts/game-full.txt` ACT 1 starts on, and for its reason: it was
# checked rather than assumed (the market hall covers x −47..−15, z −13..13, this does not), and
# a hook fired from here really bites. `look 180 66.6` puts the anchor **up and behind him at
# yaw 180 = +Z**, so `S` walks him straight away from it — which is §3D's own sentence.
# ⚠️ **`S` points at −Z from here and that is deliberate**: measured 2026-08-26, the +Z side of
# this spot is blocked (a walk into it reads 0.915 m/s instead of 6.000), so a run that walked
# the other way would report the wall and call it the rope. The anchor this bites — body 149 at
# (45.00, 34.25, −29.00) — is up and at +Z, so „weg vom Anker" IS the clear direction.
look 180 66.6
warp 45 0 -43
wait 0.5                # the warp takes effect one tick later than it is written
mark f176-standing
assert speed < 7        # on his LEGS, not still falling: this is the state R1 is about

# ⚠️ **Held for exactly as long as ACT 2 needs it.** The `hook` command's second number is how
# long the trigger stays down, and letting it go IS this run's control — 0.5 s of settling plus
# ACT 2's second, and no more (`scripts/f006-drive.txt` learned the other half of this the hard
# way: an act that outlives its trigger measures a free fall).
# ⚠️ **3.0 s and not 1.6 since 2026-08-26**: ACT 2b was added between ACT 2 and ACT 3 and it
# needs the rope for another 1.3 s. The release still lands before ACT 3 asks for it.
hook right 3.0
wait 0.5
mark f176-hooked
assert rope > 0         # it bit; the log line above says on which body and at what distance

# ---- ACT 2 · R1 · „seil ist vorne und ich laufe zurueck" ------------------------------------
# ⚠️ **1.5 s of key for 1.0 s of measurement, and that is not slack.** `ground_step` returns the
# desired velocity directly below the run speed, so a walk stops in the SAME tick the key does —
# a first version held `S` for exactly 1.0 s and both marks landed two ticks after it, measuring
# a player who had already stopped. ACT 3 read 0.000 m/s and caught it (§6 rule 5).
# ⚠️ **And it must not reach INTO act 3 either.** A second version held 1.5 s in both acts, so
# ACT 2's release landed inside ACT 3's hold and cancelled it — 0.000 m/s again, from the
# opposite mistake. 1.2 s covers the mark at +1.0 s and is over before the next `key` is written.
key S 1.2
wait 1.0
mark f176-a-second-of-s-on-the-rope
# The whole claim in one band. `run_speed_m_s` is 6.0, so anything at or over it is a player who
# walked straight out of his own rope — which is what this used to read.
assert speed < 2.0
assert rope > 0         # and the rope is still there: R1 is a clamp on the legs, not a release

# ---- ACT 2b · §3F · „zur seite gehen" ON A TAUT ROPE, AND IT IS A SWING -----------------------
# 🔴 **RE-DERIVED 2026-08-27, and the header above says why it had to be.** This act used to
# assert `height < 1` and `speed < 7` — *"a walking man"* — written against the reverted build
# to catch `FIND-182`'s ground elevator (`player::locomotion::rope_taut_brake`, 250 m/s² straight
# UP the rope, gone since 2026-08-26). **The elevator it was aimed at no longer exists**, and the
# ceiling it left behind forbids the feature `Q-058` was built for.
#
# The stance, spelled out: the anchor is body 149 at (45.00, 34.25, −29.00), the player stands at
# (45, ~0.3, −43). That is **34.0 m up and 14.0 m along +Z, i.e. 36.72 m away — and the rope is
# born at exactly that distance**, so it is TAUT on the first tick. Pressing `A` therefore moves
# him along a sphere centred 34 m over his head. **Sideways on that sphere is upwards.** The
# joint holds the radius to the millimetre (288-cell matrix: worst excess 0.0050 m), which is
# *„aber NICHT das seil verlängern!!"* met exactly — and the height is a consequence of the
# geometry, not a leak.
#
# Measured 2026-08-27 [offlinebot], debug build, one second of `A` from a CLEAN taut stance
# (`tests/vector_rope.rs::probe_ground_walk_on_a_rope`, no `key S` before it):
#
#                        y at +0.2 s      at +0.6 s        distance to the anchor
#   Drive, this build     0.343 m          1.555 m          36.723 m, every tick, unchanged
#   Pendulum, control     0.322 m          0.431 m          36.723 m, every tick, unchanged
#
# ⚠️ **What replaces the two lines is ONE assert, and it is derived from `game.ron`, not from the
# measurement.** `rope_drive` clamps its target to `vector.drive_speed_m_s` = 52 m/s and the free
# pull adds `drive_idle_speed_m_s` = 12 along the rope axis, so **55 m/s is the ceiling this
# mechanism has.** A radial runaway of the `FIND-182` shape is an *acceleration* with no ceiling
# at all — 250 m/s² held for a second is 250 m/s, which the avian clamp shows as
# `vector.max_speed_m_s` = 75 — so this line still catches exactly the thing the old pair was
# written for, and it stops forbidding the swing.
wait 0.3                # ACT 2's `key S` is over at +1.7 s; this is +1.8 s
key A 1.2
wait 1.0
mark f176-a-second-of-a-on-the-rope
assert speed < 55       # 🔴 the DRIVE is what moves him: drive_speed_m_s 52 + drive_idle 12.
                        #    An unbounded radial brake shows up here as max_speed_m_s = 75.
assert speed > 3        # the control: he really is moving, he is not stuck against something
assert rope > 0

# ---- ACT 3 · THE CONTROL · the same key with nothing holding him ----------------------------
# ⚠️ The trigger above ran out at the end of ACT 2b — the thing this number is supposed to be
# measuring is now deleted (§6 rule 5, second half).
wait 0.5
assert rope < 1
key S 1.2
wait 1.0
mark f176-the-same-second-of-s-with-no-rope
# If this is not the run speed, ACT 2 was measuring a player who cannot walk at all.
assert speed > 4.0
