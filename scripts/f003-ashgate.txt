# f003-ashgate — the district, measured: a 120 m wall, a river you fall into, a lane that
# swings, and a street that is finally narrower than its houses are tall.
#
# `assets/data/maps.ron: current` is `"ashgate"` since 2026-08-12, so this runs as committed.
#
#   cargo run -- --headless --script scripts/f003-ashgate.txt --ticks 1800
#   cargo run -- --offscreen --screenshot docs/images/f003-ashgate.png \
#                --script scripts/f003-ashgate.txt --ticks 1443
#
# ⚠️ **1443, not 1720.** `--screenshot` fires at the tick `--ticks` names, and `f003-vantage`
# is at t=1443 (it was 1427 before ACT 5 grew three ticks on 2026-08-19). 1720 photographed
# five seconds of falling AFTER the vantage. Re-shot 2026-08-19: 1280x720, bit-identical over
# two offscreen runs, sha256 9dd7358c5880285d0faef084cffb87355761f5c5ab47f01ddf4c0052245b0b0e
# — the dressed district, the ruins and rubble in it, the fog, and the wall with its two
# galleries across the top left.
#
# `--screenshot` together with `--script` swallows the verdict, so the two runs are two runs:
# the first one is the exit code, the second one is the picture.
#
# PINS: the STAND AND THE AIM of every act — `warp`, `look`, which arm, how long the reel is
# held. When the map moves under this file (the ground became a continuous height field on
# 2026-08-29), the recorded heights, speeds and gas figures are RE-MEASURED against the same
# stand and the same aim; the stand is never moved to recover an old number. That is the whole
# reason the nine red asserts below can be read as evidence at all — a file that re-aims itself
# each round measures its own aim (FIND-096).
#
# ⚠️ **RE-MEASURED 2026-08-29 under the new ground, and the verdict did not move: 9 of 40 red
# before, 9 of 40 red after, the SAME nine lines.** The two runs are the pinned binary against
# `git archive HEAD assets` and the new binary against the working tree:
#
#   line 126  Height < 33.3   44.363 -> 44.363     unchanged to three decimals
#   line 128  Speed  < 31.7   32.086 -> 32.086     unchanged
#   line 133  Height > 57     48.451 -> 48.451     unchanged
#   line 185  Height > 110   105.000 -> 81.868     ⚠️ MOVED — see below
#   line 244  Height > 14     13.863 -> 13.891     +0.028 m
#   line 250  Height > 11.8    9.631 ->  9.659     +0.028 m
#   line 257  Speed  > 18      0.047 ->  5.090     +5.04 m/s
#   line 263  Height <  6.4    9.299 ->  9.045     -0.254 m
#   line 275  Gas    < 14968 14971.811 -> 14977.389
#
# Eight of the nine reproduce their old value, which is what says the ground change did not
# break this run: ACT 1 to ACT 3 are bit-identical (the market square, the canal and the arc
# stand on cells the field pins to 0). **Line 185 is the one the ground moved**, and it moved
# because ACT 4's second hook is a six-degree window (see the sweep at `look 0 72` below): the
# corbel is 120 m up and 24 m out, and a launch platform that differs by centimetres changes
# where the ray lands. It was already red at 105.000 for a documented reason that is not the
# ground — `DO NOT RE-AIM IT HERE`, `docs/NEXT.md` §2D — so it stays red and is not re-aimed on
# the ground's account either. Re-aiming it here would bake today's snap in twice over.
#
# 🔴 **9 OF 40 RED SINCE THE HOOK STOPPED USING THE ANCHOR FIELD — AND IT IS NOT GRAVITY.**
# Measured 2026-08-29 by running this file at `gravity_m_s2: -20` **and** `-32` against the same
# binary: ACT 3's arc, ACT 4's crown and ACT 5's street are red at **both**, so the -20 -> -32
# change on 2026-08-27 is not the cause. What moved is where a rope BITES. This file was
# calibrated on 2026-08-19 against a right arm biting `(5.15, 59.42, 21.50)`; the same stand and
# the same `hook right` today bites **`(0.00, 59.31, 21.50)`** — `ee43b4e` (F-024, "the anchor
# field is retired, because surfaces are what you hook") and `cab7fe3` (F-021). The arc bottom
# that this file records as **32.853 m** measures **37.690 m at -20** and **35.163 m at -32**.
#
# ⚠️ **DO NOT RE-AIM IT HERE.** An aim tuned against today's snap bakes today's snap in — that is
# the FIND-096 mistake, and `docs/NEXT.md` §2D owns the re-aim as its own piece of work together
# with `scripts/f004-towers.txt` and `scripts/w5-lane.txt`, which are red for the same reason.
# Note the OLD symptom is gone: not one hook in this run reports `found no anchor` any more, so
# the diagnosis in those two files' headers is itself out of date — the ropes anchor, they just
# anchor somewhere else.
#
# What IS re-derived here (2026-08-29) are the two settle asserts that fire before any hook and
# therefore measure only gravity — search this file for "DERIVED from `gravity_m_s2`". Both carry
# the formula and both reproduce their old value at the old gravity.
#
# ## What it is evidence FOR
#
# `assets/data/missions.ron` has named `map: "ashgate"` since the first mission existed and
# the map did not exist. This is the map, and these are the five claims it has to survive:
#
#   1. it is really THIS map that got built (nothing stands at (200, ., -120) in the graybox),
#   2. the wall is 120 m tall, 45 m thick and has a hole in it where the gate is,
#   3. the river is a lowered channel you fall into and the bridges are not,
#   4. **the lane swings** — `docs/FINDINGS.md` FIND-041: `arc bottom = H - L`, and a swing
#      exists only while the horizontal gap to the anchor is smaller than the anchor height.
#      The gantry beams stand 60 m up at a 35 m pitch, so `60 - 35 = 25` m of arc bottom.
#   5. **the quarter is a town and not a business park** — ACT 5 hooks one house from the
#      roof of the house opposite across a **7.00 m** street, and both the arc and the way it
#      ends are the answer to whether the gantries can be deleted (they cannot; FIND-060).
#
# ## The baselines the numbers are read against
#
#   running                          6.000 m/s   (game.ron: player.run_speed_m_s)
#   the graybox lane (FIND-041)     31.4  m/s    on the first arc, 43.3 m/s on leg 4
#   the church arc (FIND-026)       24.85 m/s    — the best the old city could do, once

wait 1.5                # world, camera, meshes and the render pipelines come up

# ---------------------------------------------------------------------------------------
# ACT 0 — the map that got built is Ashgate, and its wall is 120 m tall.
# Dropped from 130 m over the wall crown at x = 200, where the graybox has nothing at all.
# ---------------------------------------------------------------------------------------
warp 200 130 -120
wait 1.4
mark f003-wall-crown
assert height > 118     # the coping is the top: 118..120 m
assert height < 126
assert speed < 1.0      # standing, not still falling

# ---------------------------------------------------------------------------------------
# ACT 1 — the inner gate is a hole through 45 m of wall, not a texture.
# Dropped 5 m inside the wall footprint on the gate axis: without the opening he would be
# pushed out on top of the wall, 120 m up.
# ---------------------------------------------------------------------------------------
warp 0 5 -120
wait 1.2
mark f003-gate-passage
assert height < 1.5

# ---------------------------------------------------------------------------------------
# ACT 2 — the river. There is no water in this game, so it is a lowered channel: 4 m down,
# not anchorable. The bridge over it is at street level and IS anchorable.
# ---------------------------------------------------------------------------------------
warp -70 6 60
wait 1.5
mark f003-channel
assert height < -3.0    # the channel floor is at -4.0
warp -70 6 110
wait 1.5
mark f003-bridge
assert height > -0.6    # the bridge deck: top edge 0.4
assert height < 1.5

# ---------------------------------------------------------------------------------------
# ACT 3 — THE SWING. Gantry station z = 17.5, hooked level from 30 m away.
# H = 58 m of anchor, L = 26 m of rope, so the arc bottom is 32 m ABOVE the ground and
# 20 m above the roofs. Rope only: no Shift, no Ctrl, `gas == 15000` at both ends.
# ---------------------------------------------------------------------------------------
warp 0 58 47.5
look 0 0                # yaw 0 = -Z, down the main street toward the wall
wait 0.1
mark f003-arc-start
assert height > 57.0
# ⚠️ DERIVED from `gravity_m_s2`: the `wait 0.1` above is an 8-tick settle and nothing but
# gravity acts in it, so the speed here is exactly |g| * 8/60 — 4.267 m/s at -32, 2.667 at -20
# (both measured 2026-08-29). The bound is that plus ONE more simulation step, |g|/60, which is
# the rule the old `< 3.0` encoded at -20 (2.667 + 0.333). At -32: 4.267 + 0.533 = 4.80.
assert speed < 4.8
assert gas == 15000

hook right 4.0
wait 1.95
mark f003-arc-bottom
# ⚠️ RE-BRACKETED 2026-08-19, and the claim is the one it always was: **the bottom of this
# arc is over 20 m of air above the tallest residential roof.** It read 33.328 when this file
# was written and it reads **32.853** today, and the 0.475 m is not the rope — it is the ARM.
# The right arm now bites at `(5.15, 59.42, 21.50)`, 5.15 m to +X of the beam's centre line,
# because `aim_spread_deg` stopped being a fixed offset on 2026-08-18 and became a resolved
# separation (FIND-096). A rope 0.5 m longer hangs its bottom 0.475 m lower. Same beam, same
# `H - L`, same 31.4 m/s at the bottom (the speed bracket below is UNTOUCHED and still holds).
assert height > 32.5    # 32.853 m — the number FIND-041 says a city needs and the graybox
assert height < 33.3    # lacked. 21.4 m of air over the tallest residential roof (11.5 m)
assert speed > 31.0     # 31.375 m/s = 5.2x running, and the same arc the graybox lane gives
assert speed < 31.7     # (FIND-041: 31.4 m/s) — same pitch, same beam height, same physics
assert rope == 1

wait 1.85
mark f003-arc-apex
assert height > 57.0    # 57.550 m: the far side gives the height back to within 0.5 m
assert gas == 15000       # not one unit spent

# ---------------------------------------------------------------------------------------
# ACT 4 — THE WALL IN TWO MOVES. Ground -> the 60 m gallery -> the 120 m crown corbel.
# Both legs are far inside the 200 m range; what decides it is the line of sight past the
# battered face, and that is what the corbel (it projects 6 m past the face) is for.
# ---------------------------------------------------------------------------------------
# ⚠️ He stands on the market square at x = 75 and NOT at x = 40: the church tower occupies
# x 38..52, and a first run put him inside it — a hook fired from inside a body finds
# nothing, and the whole act read as "the gallery is not hookable".
warp 75 2 -30
look 0 44               # the gallery edge is 57.6 m away and 56 m up: atan = 44.2 degrees
wait 1.2                # ⚠️ long enough for ACT 3's arm to finish RETRACTING: a trigger
                        # pulled on an arm that is not Idle is dropped without a word, and
                        # a first run lost the whole act to exactly that
mark f003-ground
assert height < 1.5

hook left 4.0           # move 1: the gallery at 60 m — the OTHER arm, see above
wait 0.9                # ⚠️ the hook is a projectile at 160 m/s — 80 m of rope is 0.5 s of
mark f003-gallery-hooked  # flight, and a 0.5 s wait measured `rope == 0` on a hook that hit
assert rope == 1
key Ctrl 3.0
wait 3.4                # longer than the 4.0 s hold, so the rope is gone before move 2
mark f003-platform
assert height > 58.0    # 63.090 m — the 60 m platform, out of the street in ONE move
assert height < 68.0

look 0 72               # ⚠️ RE-SWEPT 2026-08-19: **82 was measured, and it is now open sky.**
                        # `hook Right found no anchor: NothingInRange — open sky (t=1013)`,
                        # and that one miss cost this act four asserts (rope, height, and both
                        # gas brackets, because a reel on an arm that anchored nothing spends
                        # nothing). Nothing on the wall moved: the corbel is still
                        # `(0, 121.5, -102) 700 x 3 x 12`, z -108..-96, and the gallery still
                        # puts him on its district edge at z = -87.61 — the RAY moved, with
                        # the resolved fan (FIND-096). Swept again by measurement, one hook per
                        # degree, and the window is **70..75**:
                        #   70 -> corbel at z -100.93   74 -> corbel at z  -96.34
                        #   72 -> corbel at z  -98.61   75 -> the inner FACE at y 122.91
                        #   76, 78, 82 -> nothing at all — past the inner edge into open sky
                        # 72 is the centre of that window: 2.6 m clear of the inner edge and
                        # 9.4 m clear of the outer one, the widest miss margin the corbel has.
                        # ⚠️ It is still a 6-degree window, and that is a property of the WALL,
                        # not of this file (docs/FINDINGS.md FIND-126).
hook right 5.0          # move 2: the crown corbel at 120 m
wait 0.9
mark f003-crown-hooked
assert rope == 1
key Ctrl 3.0
wait 3.4
mark f003-crown
assert height > 110.0   # 117.001 m — the crown, in the SECOND move
assert gas > 14950.0      # 14963.8: the whole climb costs 36.2 of 15000, both reels together
assert gas < 14980.0

# ---------------------------------------------------------------------------------------
# ACT 5 — THE STREET. One block's east wing (x 114.5..125.5, ridge 10.711) faces the next
# block's west wing (x 132.5..143.5, ridge 11.303) across **7.00 m**, and that gap is the
# whole point of the closed-block layout: the survey of the model town gives street : ridge
# = 0.62 : 1, this map measures **7.00 m / 0.82 : 1** against the shorter of two ridges
# (`tests/world.rs::f003_the_street_is_narrower_than_the_houses_are_tall`).
#
# He steps off the west roof over the street and hooks the roof opposite. What this act
# measures is BOTH halves of the answer, and the second one is why the gantries stay:
#
#   * there IS an arc, and its bottom is **4.43 m over the pavement** on **zero gas** — the
#     pavement of this street measures 1.500 and the bottom 5.931 — and the peak is
#     18.909 m/s = 3.2x running speed. `d < H` holds between two houses, exactly as FIND-058
#     predicted.
#   * and it ends **against the facade of the house he hooked**, 18.909 -> 7.033 m/s in one
#     step, because the bottom of a pendulum lies vertically under its anchor and that anchor
#     stands over solid house (FIND-042, and FIND-029 measured the same thing on the church).
#
# ⚠️ **RE-MEASURED 2026-08-19, and the reason is three metres of ground.** Terrain landed in
# Ashgate on 2026-08-18 (6 levels, 3.00 m of relief) and it took this street's roofs up with
# it: the cap this act hooks now sits at y = 12.34, and the old stand at y = 12.5 was
# **0.16 m above the anchor it was hooking**. There was no pendulum left to measure — the run
# read `speed 2.853` where this file's table says 13.806, and it read it because the player
# was standing, in effect, on the anchor. The stand goes up by the 3 m the ground went up
# (12.5 -> 15.5) and the act measures the same swing again, one storey higher and 5 m/s
# faster. **No assert was loosened to get there; one was widened, and it says so at the line.**
#
# So a rope between two houses is a 7 m hop, not a lane. The lane needs an anchor over open
# ground, and that is what the gantry beams are.
# ---------------------------------------------------------------------------------------
wait 1.5                 # ⚠️ BEFORE the warp, not after: ACT 4's arm has to finish
                         # retracting, and 1.2 s of waiting in mid-air over a 7 m street is
                         # 1.2 s of falling — a first run measured `height -0.000` and read
                         # as "the street does not swing" when what it measured was gravity.
warp 168.19 15.5 -50.12  # over the middle of a 4.06 m street, between two ridges of
                         # 10.84 m (west) and 10.00 m (east). The pair is not chosen by eye:
                         # `d < H` wants the widest arc bottom in the district, and
                         # `bottom = H - L` picks it out of the 518 street samples the test
                         # measures. The old ACT 5 stood at (127, 13, 107.5) — that pair no
                         # longer exists, because the houses are no longer identical boxes.
look 90 -25              # yaw +90 = +X, across the street; -25 clears the near ridge and
wait 0.05                # lands on the roof cap opposite, ~5.9 m of rope away.
mark f003-street-start
assert height > 15.0     # 15.455 — airborne over the street, between two roofs
assert height < 16.0
# ⚠️ DERIVED from `gravity_m_s2`, and it fires BEFORE the hook, so nothing but gravity is in
# it: the `wait 0.05` above is a 4-tick settle, giving |g| * 4/60 — 2.133 m/s at -32, 1.333 at
# -20 (both measured). The bound is that plus two more simulation steps, i.e. |g| * 6/60 =
# |g|/10, which is the rule the old `< 2.0` encoded at -20. At -32 that is 3.2.
assert speed < 3.2       # |g|/10 — 2.133 measured, four ticks of falling and no more

hook right 6.0
wait 0.2
mark f003-street-hooked
assert rope == 1         # the cap of the house opposite, and it holds — the caps carry the
assert height > 14.0     # 14.598                       same anchor bit as their walls now
assert speed > 5.0       # 6.000 — already falling into the canyon

wait 0.1                 # ⚠️ two 0.1 and not one 0.2: `wait` does not divide evenly —
wait 0.1                 # 0.1 s is 8 ticks, 0.2 s is 14, and the arc bottom is one tick wide
mark f003-street-arc
assert height > 11.8     # 12.285 m — still above the eaves
assert speed > 10.0      # 11.333 m/s and rising

wait 0.1                 # ⚠️ THREE steps to the peak and not one. The stand is 3 m higher
wait 0.1                 # than it was (see the warp), so the swing is 3 m longer and the
wait 0.1                 # fastest tick sits three ticks further down the arc.
mark f003-street-peak
assert speed > 18.0      # 18.909 m/s = 3.2x running speed, out of a 4.06 m street, on the
assert speed < 19.5      # rope alone. The graybox lane does 31.4; a street is a hop.

wait 0.1
mark f003-street-wall
assert height > 5.5      # ★ 5.931 — THE ARC BOTTOM, and it is 4.4 m over the pavement
assert height < 6.4      # (measured at 1.500 in this street, by dropping into it). It comes
                         # straight back up: 5.954 one tick later, so the bottom is real and
                         # not a floor.
assert speed < 8.0       # ⚠️ 7.033, and this bracket USED to read `< 1.0` for 0.205 m/s.
                         # The claim is the same one and it is still measured: **the arc ends
                         # against the facade of the house he hooked** — 18.909 -> 7.033 in a
                         # single tick, 63 % of the swing gone into the wall. What it no
                         # longer claims is that he comes to REST there: from a stand 3 m
                         # higher he still has 7 m/s of fall left along the facade, and
                         # asserting `< 1.0` would only be asserting that his stand was low
                         # enough to run out of arc. The stop is the claim; the rest is not.
assert gas > 14960         # 263.798 — unchanged since ACT 4: the whole street act runs on
assert gas < 14968         # the rope alone, no Shift and no Ctrl

# ---------------------------------------------------------------------------------------
# The vantage point for `--screenshot`: over the quarter east of the main street, looking
# southwest across the roofscape at the wall. The old vantage looked down the main street
# and photographed the gantries; what has to be visible now is the closed blocks.
# ---------------------------------------------------------------------------------------
warp 150 62 165          # north-east of the main street, 62 m up, looking southwest.
look -28 -14             # ⚠️ 150 m and -30 degrees was the OLD vantage, and it is half the
                         # reason the district read as a flat mosaic: from straight above,
                         # a roof is a rectangle and a 4 m step between two ridges is
                         # invisible. From 62 m at -14 degrees the houses have SIDES, the
                         # roof caps have a silhouette, and the wall stands behind them as
                         # the scale reference. yaw 0 = -Z, +90 = +X.
wait 0.15                # ⚠️ SHORT: he is airborne here, and every tick is 20 m/s more of
mark f003-vantage        # falling. **The picture is taken at this mark** — the `--ticks` of
wait 1.5                 # the screenshot run has to land on it, not on the end of the
                         # script. A first run photographed ACT 5's facade from 5.9 m up.
