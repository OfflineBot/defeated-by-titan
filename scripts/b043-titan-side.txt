# b043-titan-side — does a hook anchor in the AIR NEXT TO a drawn titan? (B-043 cand. 3,
# the opposite sign of B-042.) Measurement run: the verdict is in the `anchored ... at x y z`
# log lines, classified by position — an anchor near the titan's axis is a titan bite, an
# anchor at y ~ 0.05 far down-range is the street the ray fell onto after missing, an anchor
# elsewhere is whatever distant block the ray rose into.
#
#   ./target/debug/defeated_by_titan --headless --script scripts/b043-titan-side.txt --ticks 1600
#
# Husk (medium class, 20 m tall): collider = two axis-centred capsules (titan::rig), torso
# r 2.00 m (cylinder y 2.0..14.4, caps to 0..16.4), head r 1.10 m (y 15.3..18.9) —
# rotationally symmetric, so the whole question is "lateral offset from the axis". The drawn
# body at scale 2.0 is 'hager': the lower legs are two thin columns nowhere near the 2 m
# capsule. Nine lanes along the boulevard (each leg: player at (x0, 0.2, -76), husk spawned
# 40 m west at (x0-40, 0, -76)), ray west with a lateral z offset; the husk walks ~3 m/s so
# each hook flies 0.35 s after its look (B-041) and < 2 m of approach — offsets stay lateral.
# Capsule prediction: ankle-height bites at |z-off| < ~1.94, chest at < 2.00, head at < 1.10.
settings assist_catch 0
settings assist_strength 0
wait 1.0

# ---- ankle height (aim y 1.0), offsets 0.8 / 1.3 / 1.7 / 2.3 -----------------------------
warp -300 0.2 -76
wait 0.7
spawn titan husk -340 0 -76
look 91.15 -1.0
wait 0.35
hook right 0.6
wait 0.4
mark b043-ts-ankle-z08
wait 0.5

warp -230 0.2 -76
wait 0.7
spawn titan husk -270 0 -76
look 91.86 -1.0
wait 0.35
hook right 0.6
wait 0.4
mark b043-ts-ankle-z13
wait 0.5

warp -160 0.2 -76
wait 0.7
spawn titan husk -200 0 -76
look 92.43 -1.0
wait 0.35
hook right 0.6
wait 0.4
mark b043-ts-ankle-z17
wait 0.5

warp -90 0.2 -76
wait 0.7
spawn titan husk -130 0 -76
look 93.29 -1.0
wait 0.35
hook right 0.6
wait 0.4
mark b043-ts-ankle-z23
wait 0.5

# ---- chest height (aim y 10), offsets 1.7 / 2.3 / 2.9 ------------------------------------
warp -20 0.2 -76
wait 0.7
spawn titan husk -60 0 -76
look 92.43 11.9
wait 0.35
hook right 0.6
wait 0.4
mark b043-ts-chest-z17
wait 0.5

warp 50 0.2 -76
wait 0.7
spawn titan husk 10 0 -76
look 93.29 11.9
wait 0.35
hook right 0.6
wait 0.4
mark b043-ts-chest-z23
wait 0.5

warp 120 0.2 -76
wait 0.7
spawn titan husk 80 0 -76
look 94.15 11.9
wait 0.35
hook right 0.6
wait 0.4
mark b043-ts-chest-z29
wait 0.5

# ---- head height (aim y 17), offsets 0.8 / 1.6 -------------------------------------------
warp 190 0.2 -76
wait 0.7
spawn titan husk 150 0 -76
look 91.15 21.0
wait 0.35
hook right 0.6
wait 0.4
mark b043-ts-head-z08
wait 0.5

warp 260 0.2 -76
wait 0.7
spawn titan husk 220 0 -76
look 92.29 21.0
wait 0.35
hook right 0.6
wait 0.4
mark b043-ts-head-z16
wait 0.5
