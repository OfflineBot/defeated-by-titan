# f004-towers — the swing lane, measured: one real arc free-aimed, and then three of them
# chained on the assist. **31 asserts, 838 ticks, exit 0 (2026-08-19).**
#
# 🔴 **5 OF 31 RED SINCE 2026-08-20, AND THE AIM IS THE CAUSE — do not re-derive the angles.**
# ⚠️ **RE-MEASURED 2026-08-29 AND THE DIAGNOSIS BELOW IS OUT OF DATE.** The symptom this header
# was written around — `found no anchor: NothingInRange — open sky` — **no longer occurs**: not
# one hook in this run fails now (`grep -c 'found no anchor'` = 0). The ropes anchor; they anchor
# somewhere ELSE. `ee43b4e` (F-024, "the anchor field is retired, because surfaces are what you
# hook") moved the bite point, and the arc that this file records at 32.853 m now bottoms at
# 35.163 m. Ten of its asserts are red at `gravity_m_s2: -20` **and** at `-32` against the same
# binary, so the -20 -> -32 change of 2026-08-27 is NOT the cause of any of them.
# The one line that WAS gravity is the settle assert in ACT 1 — search for
# "DERIVED from `gravity_m_s2`"; it now carries its formula and reproduces `< 3.0` at -20.
# ⚠️ Still: **do not re-aim it here.** -> `docs/NEXT.md` §2D, with `f003-ashgate` and `w5-lane`.
#
# `F-024` (1450bfc, "the snap searches sideways only") removed the snap's vertical correction:
# its own commit measures *"largest vertical deviation a snap can produce 9.232 deg / 3.414 m ->
# 0.000006 deg / 0.0000 m"*. Every `look` line here was aimed under the 2D cone. One shot now
# reports `found no anchor: NothingInRange — open sky (t=721)`. The numbers in this file are
# unevidenced until it is re-aimed, and an aim tuned against today's snap bakes today's snap in
# (the FIND-096 mistake). -> docs/FINDINGS.md FIND-145.
#
#   cargo run -- --headless --script scripts/f004-towers.txt --ticks 1200
#
# ⚠️ **RE-MEASURED 2026-08-19 after 14 of its 39 asserts went red, and the file is SHORTER
# than it was.** Three things had moved under it and none of them was the rope:
#   1. **terrain** — the lane floor is at 2.400, and ACT 0 was warping the player 1.9 m
#      inside it, where `key W` does nothing at all;
#   2. **the fan** — every `look 0 NN` in ACT 2 was hand-compensated for a fixed ±28 degree
#      side offset that stopped existing on 2026-08-18 (FIND-096). ACT 2 is now aimed with
#      `assist_strength 100` and is immune to the next spread change; ACT 1 stays free aim;
#   3. **the swing room** — with the floor 4 m higher the chain gets three legs, not five,
#      and legs 4 and 5 are DELETED rather than loosened. The measurements that killed them
#      are written out at ACT 2 and in docs/FINDINGS.md FIND-126.
#
# ⚠️ Rope only. This run never presses Shift (boost) and never presses Ctrl (reel), and it
# spends **zero gas** — `assert gas == 15000` at the end is the proof. Every metre of height and
# every metre per second below comes out of `player/rope.rs` and gravity, nothing else.
#
# ## What it is evidence FOR
#
# `docs/FINDINGS.md` FIND-026: "the city lacks usable arcs — the arc bottom lies underground
# for every rope longer than the anchor's height, and the church is the only anchor tall enough
# to give a real one". The user, after playing it: „und mehr tuerme zum lang hangeln!".
#
# `assets/data/maps.ron` answers that with a line of gantries at a 35 m pitch, and the arithmetic
# behind them stands in that file. The one line of it that decides everything:
#
#     arc bottom = H - L        (H = anchor height, L = rope length at the hook)
#
# so a swing exists only while `L < H`, and since `L` is at least the horizontal distance to
# the anchor, **a tower has to be taller than the gap to the next tower.** 200 m of
# `hook_range_m` does not change that by one metre — it is not the range that is missing.
#
# ---------------------------------------------------------------------------------------
# ⚠️ **RE-AIMED ONTO ASHGATE, 2026-08-12 — AND NOT ONE ASSERT MOVED.**
#
# This file was cut against the graybox's nine gate towers at z = 70, and it swung along **+X**
# from (-30, 58, 70). `maps.ron: current` is `"ashgate"` since `F-003`, which has no gate
# towers, so all five hooks found nothing and the run stood at **17 of 39 red**.
#
# Ashgate has the same lane, turned ninety degrees: the **gantry line over the main street**,
# `maps.ron: ashgate` — "ten stations from z = 227.5 to -87.5 = 315 m of lane", crossbeams
# `(0, 58, z)` of `44 x 4 x 8` at a **35 m pitch**, `anchorable: true`, carried on legs at
# x = ±14 that stop at y = 56 and are not anchorable. So the run now starts at
# `(0, 58, 257.5)` and swings along **-Z** (`look 0 0`; yaw 0 is -Z, `docs/conventions.md`).
#
# **The only thing that changed is the two warps and the yaw of five `look` lines.** Every
# assert, every bracket and every wait is byte-identical to the graybox version, because the
# geometry is: same 35 m pitch, same beam height. Measured on ashgate against the graybox
# numbers this file has quoted since it was written:
#
#   leg                graybox            ashgate           bracket
#   arc bottom         33.32 m / 31.4     33.328 / 31.376   33.0-33.7 / 31.0-31.7
#   leg 1              33.41 m / 31.32    33.411 / 31.321   33.0-33.8 / 31.0-31.8
#   leg 2              27.66 m / 34.78    27.660 / 34.784   27.3-28.0 / 34.4-35.2
#   leg 3              19.30 m / 39.26    19.357 / 39.256   19.0-19.7 / 39.0-39.6
#   leg 4              12.63 m / 42.52    12.629 / 42.523   12.3-13.0 / 42.2-42.9
#   leg 5 caught       15.08 m / 41.35    15.109 / 41.339   14.5-15.6 / 40.9-41.8
#
# and the five anchors come back at 59.42, 56.25, 58.70, 57.75, 59.03 against the graybox's
# 59.42, 56.25, 58.70, 57.76, 59.04 — sub-centimetre, on a different map. That is not a
# coincidence to be admired, it is the check: **the claim was about the arithmetic of a
# 35 m-pitch lane at 58 m, not about a particular row of boxes**, and it survived being moved
# to a different row of boxes without a single bracket being widened.
# ---------------------------------------------------------------------------------------
#
# ## The baselines every number here is read against
#
#   running                       6.000 m/s   (game.ron: player.run_speed_m_s)
#   the church arc (FIND-026)    24.85  m/s   — the best the old city could do, once
#   a clean rig swing (FIND-026) 24.119 m/s
#
# ## ACT 1 — one arc, and where its bottom is
#
# The crossbeam at z = 227.5 is hooked from the air one station behind it, level with it: anchor
# (0.00, 59.42, 231.50), rope **26.10 m**. `59.42 - 26.10 = 33.32`, and 33.32 m is what the
# arc bottom measures — **33 m above the ground and 22 m above the residential roofs**, not
# 24 m underground. Peak **31.4 m/s at the bottom**, and the far side of the swing gives the
# height back to within 0.4 m: the rope is not where the energy goes.
#
# ## ACT 2 — the chain, which is the actual commission
#
# Five hooks, five arcs, one after the other. Anchor, rope, lowest point, peak speed:
#
#   leg 1  beam 227.5  (231.50, 59.42)  L 26.10  low 33.41 m  31.3 m/s
#   leg 2  beam 192.5  (196.50, 56.25)  L 33.89  low 27.66 m  34.8 m/s
#   leg 3  beam 157.5  (161.50, 58.70)  L 41.79  low 19.36 m  39.3 m/s
#   leg 4  beam 122.5  (126.50, 57.75)  L 47.96  low 11.03 m  43.3 m/s  = 7.2x running
#   leg 5  beam  87.5  ( 91.50, 59.03)  L 45.27  caught at 15.1 m and 41.3 m/s, climbs to 43.1 m
#
# The along-lane positions are not measured directly — `Metric` has no z — but they follow from
# the anchor, the rope and the height, and the bottom of an arc lies under its anchor: z = 257.5
# at the start, 91.5 at the last hook, ~49 at the top of leg 5. **208 m of chained swinging in
# 7.23 s (t=508 to t=942) = 28.7 m/s average, 4.8x running speed, and not one unit of gas.**
#
# The lowest point sinks from leg to leg because a release at the bottom spends the whole
# height budget on speed: leg 6 out of leg 4's 11.03 m would need a 58.6 m rope on a beam
# 58 m up, `L < H` fails, and the chain would end on the ground. Leg 5 is the answer and it
# needs no gas — hold the rope past the bottom and the swing puts the height back.
#
# ⚠️ The `look` angles are not decoration and they are not round by accident: each one was
# swept until the ray hit the near face of the next crossbeam. One degree too high on leg 4
# (45 instead of 44) still hits; two degrees (46) flies over the beam and the run reports
# "found nothing anchorable" instead of failing an assert. Whoever moves a gantry in `maps.ron`
# re-sweeps these five numbers.

wait 1.5                # world, camera, meshes and the render pipelines come up

# ---------------------------------------------------------------------------------------
# ACT 0 — the baseline. What the player is worth without a rope.
# ---------------------------------------------------------------------------------------
# 🔴 **`warp 0 0.5 257.5` WARPED HIM INTO THE GROUND, and that is what broke this act on
# 2026-08-19.** Ashgate got terrain on 2026-08-18 (6 levels, 3.00 m of relief) and the floor
# of the gantry lane came up with it — it settles at **2.400** here, not at 0. A player put
# at y = 0.5 is 1.9 m INSIDE it: he does not fall, he does not walk, and `key W` moves him
# nowhere. Measured, three stands and two yaws, all `speed 0.000` after a full second of W —
# while the same W at the church (45, 1, -43) and on the boulevard (0, 2, 100) gave exactly
# 6.000 m/s in the same run. **The runner was never broken; the lane's floor moved up under
# the warp.** (docs/FINDINGS.md FIND-126.)
warp 0 6 257.5          # dropped in from above the terrain and allowed to settle onto it
look 0 0
wait 0.8
assert height > 2.0     # 2.400 — THE FLOOR OF THE LANE, and it used to read 0.050. This
assert height < 2.9     # bracket is the terrain, and it is the first thing this file proves.
assert speed < 0.5
key W 1.5               # held LONGER than the wait below — a key that expires on the same
wait 1.0                # tick as the measurement measures a player who has already let go
mark f004-running
assert speed > 6.8      # 7.042 m/s. ⚠️ `player.run_speed_m_s` is 6.000 and that is still what
assert speed < 7.3      # he runs at — the extra 1.04 is the terrain: this stretch of lane
                        # falls away under him and the descent is in the speed. The bracket
                        # was `5.9 .. 6.1` on flat graybox ground; it is the same claim about
                        # the same runner, measured on a floor that is no longer flat.
wait 0.8

# ---------------------------------------------------------------------------------------
# ACT 1 — one arc on the beam at z = 227.5, from level with its crossbeam.
# ---------------------------------------------------------------------------------------
warp 0 58 257.5
look 0 0
wait 0.1
mark f004-arc-start
assert height > 57.5    # 57.82: dropped off one station behind it, level with the beams
# ⚠️ DERIVED from `gravity_m_s2`: the `wait 0.1` above is an 8-tick settle and nothing but
# gravity acts in it, so the speed here is exactly |g| * 8/60 — 4.267 m/s at -32, 2.667 at -20
# (both measured 2026-08-29). The bound is that plus ONE more simulation step, |g|/60, which is
# the rule the old `< 3.0` encoded at -20 (2.667 + 0.333). At -32: 4.267 + 0.533 = 4.80.
assert speed < 4.8

hook right 4.0          # the rope, and nothing else, for the next four seconds
wait 1.95
mark f004-arc-bottom
# **The number FIND-026 said this city did not have.** The arc bottom is 33.32 m ABOVE the
# ground; the same rope on the old city's tallest anchor put it 24.3 m below.
# ⚠️ RE-BRACKETED 2026-08-19 and the claim is unchanged: the bottom of this arc is 30 m of
# air above the lane. 33.32 -> **32.853**, and the 0.47 m is the ARM, not the rope: the right
# arm bites at `(5.15, 59.42, 231.50)`, 5.15 m to +X of the beam's centre line, because the
# fan became a resolved separation on 2026-08-18 (FIND-096). Half a metre more rope hangs the
# bottom half a metre lower. The speed bracket is UNTOUCHED and still holds at 31.680.
assert height > 32.5
assert height < 33.3
assert speed > 31.0     # against 7.0 running and 24.85 on the church
assert speed < 31.7
assert rope == 1        # one arm, holding

wait 1.85
mark f004-arc-apex
# The far side of the swing. He is back up where he started, which is what "the rope is
# nearly lossless" means when you say it in metres instead of in percent.
assert height > 57.0    # 57.403 — the far side gives the height back to within 0.4 m, and
                        # THAT is the "nearly lossless" claim; the speed bound below is only
                        # a check that he has nearly stopped up there.
assert speed < 5.0      # 4.417. Was `< 4.0` for 3.5 m/s — same 0.5 m of extra rope as above,
                        # arriving as 0.9 m/s at the apex. Still under running speed.
assert gas == 15000       # not one unit spent: no boost, no reel

# ---------------------------------------------------------------------------------------
# ACT 2 — the chain. **THREE gantries, three arcs, and the place where it stops.**
# ---------------------------------------------------------------------------------------
#
# 🟢 **THIS ACT IS AIMED WITH THE ASSIST, ON PURPOSE, AND ACT 1 ABOVE IS NOT.** The split is
# the point of the file after 2026-08-19:
#
#   * **ACT 1 is free aim** — one level shot at one beam, `look 0 0`, no compensation in it
#     anywhere. That is a question about the MAP and it is asked with the player's own arm.
#   * **ACT 2 is `assist_strength 100`** — because its five `look 0 NN` pitches were
#     hand-compensated for a **fixed ±28 degree** side offset that stopped existing on
#     2026-08-18 (FIND-096), and the file has been red ever since: re-run free, leg 3 read
#     `1.198 m/s` where the table below said 39.26. Re-deriving those pitches would only bake
#     today's fan in and break again at the next tuning. At full snap **the pitch of a shot
#     barely matters** — a chain is aimed by RELEASE TIME, not by angle (FIND-108) — so the
#     numbers below survive the next spread change. What this act no longer proves is that a
#     player can AIM the chain; `scripts/f025-chain.txt` owns that question and measures the
#     same lane at 0 %, 50 % and 100 %.
#
# 🔴 **AND IT IS THREE LEGS NOW, NOT FIVE. That is a loss, it was measured, and it is not
# hidden here.** Legs 4 and 5 were deleted because on today's map they do not exist, not to
# make the file green. What was measured on 2026-08-19, one hook per row, assist at 100 %:
#
#   leg 4 aimed at 44 deg -> the beam TWO stations ahead, a 44 m rope, and the arc bottom
#                            goes under the raised lane floor: 10.8 -> 6.2 -> 4.0 m and he
#                            lands (27.8 -> 3.7 m/s in one tick against the terrain)
#   leg 4 aimed at 48 deg -> `anchored on body 114 at (10.89, 20.73, 161.5)` — the MAST of
#   leg 4 aimed at 52 deg -> the gantry he was just swinging on, 35 m below its beam. He
#                            stops dead against the tower: 16.6 m, 2.5 m/s.
#
# Between "the beam two stations ahead" and "the mast in front of your nose" there is no
# pitch left, because the third leg puts him at 15 m of height right beside a tower. The
# five-leg chain of 2026-08-13 ran over a lane whose floor was at y = 0 and whose fan let a
# side ray reach a nearer beam; **the lane lost the bottom 4 m of its swing room to terrain.**
# That is an answer to the tower question and it belongs to the map — docs/FINDINGS.md
# FIND-126, and the file does not pretend the two legs are still there.
#
# What the three legs DO still prove, and it is the claim FIND-026 asked for:
#
#   leg 1   31.346 m   32.615 m/s     the arc bottom, 29 m above the lane floor
#   leg 2   26.286 m   35.566 m/s   ★ faster than leg 1
#   leg 3   18.491 m   39.633 m/s   ★ faster than leg 2, 5.7x running, zero gas
#
# — a rope chain that ACCELERATES over three gantries and spends nothing. `assert rope == 1`
# stands on every leg on purpose: without it this act would measure gravity and read the same
# to three decimals (FIND-103, FIND-108).

wait 0.6
settings assist_catch 100     # ⚠️ ACT 2 ONLY. Everything above this line is free aim.
settings assist_strength 100
warp 0 58 257.5
look 0 0
wait 0.1
mark f004-chain-start

hook right 2.05         # leg 1 — beam 231.5, released at the bottom of the arc
wait 2.0                # ⚠️ the HOLD is one twentieth of a second longer than the WAIT, in
                        # all three legs. With hold == wait the mark lands on the tick the
                        # trigger expires and `assert rope == 1` reads 0.000 on a leg that
                        # swung perfectly — measured 2026-08-19, three legs, three false
                        # reds. The release is still at the bottom; the reading is 3 ticks
                        # before it instead of on it.
mark f004-leg-1
assert rope == 1        # ⚠️ the leg is a SWING and not a fall, and only this line says so
assert height > 30.8    # 31.346 m: the bottom, and the release
assert height < 31.9
assert speed > 32.1     # 32.615 m/s
assert speed < 33.2

look 0 33
hook left 1.00          # leg 2 — the next beam down the lane
wait 0.95
mark f004-leg-2
assert rope == 1
assert height > 25.7    # 26.286 m — lower than leg 1: the speed is bought with height
assert height < 26.9
assert speed > 32.7     # ★ faster than leg 1 measured. 35.566 m/s
assert speed < 36.1

look 0 39
hook right 0.95         # leg 3 — and this is where the lane runs out (see above)
wait 0.90
mark f004-leg-3
assert rope == 1
assert height > 17.9    # 18.491 m
assert height < 19.1
assert speed > 35.6     # ★ faster than leg 2. 39.633 m/s = 5.7x running speed
assert speed < 40.2

wait 1.0
mark f004-chain-end
assert gas == 15000       # 105 m of lane, three arcs, zero gas
