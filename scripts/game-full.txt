# game-full — THE WHOLE GAME, ONCE, END TO END: fly, cut, cut, cut, WON.
#
#   the verdict:  cargo run -q -- --headless  --mission tutorial \
#                     --script scripts/game-full.txt --ticks 1600
#   the picture:  cargo run -q -- --offscreen --mission tutorial \
#                     --script scripts/game-full.txt --ticks 1100 \
#                     --screenshot docs/images/f071-won.png
#
# MEASURED on offlinebot, **2026-08-29**, in **ashgate**, after the three cut acts were
# re-derived from `game.ron: gravity_m_s2` -20 -> -32 — the run says, in this order:
#
#   t=235   reel:  speed 28.741 m/s, height 15.521 m   -> `assert speed > 25` HOLDS
#   t=357   on the church roof: height 35.000 m, speed 0.000
#   t=619   cut titan 2 Torso  at 29.87 m/s
#   t=622   cut titan 2 Cortex at 30.40 m/s   -> kill: player 1 cut titan 2 — 1/3
#   t=709   cut titan 3 Cortex at 30.40 m/s   -> 2/3
#   t=796   cut titan 4 Cortex at 30.40 m/s   -> 3/3
#   MISSION WON — 3/3 kills, and `assert health > 0` still holds at the end
#   headless: **24 asserts held, 1098 ticks, exit 0**
#
# ⚠️ **The three cut ticks moved (656/777/898 -> 622/709/796) and the closing speed did not
# (30.33 -> 30.40 m/s).** That is the whole shape of the 2026-08-29 re-derivation: the pass is
# pinned to a SPEED and gravity decides how long it takes to get there. The graze-to-nape gap is
# still three ticks. The four acts are 87 ticks apart instead of 121.
#
#   (23 asserts until 2026-08-19; the 24th is the invocation guard at line 102 and it is an
#    ADDED claim, not a changed one)
#
# ⚠️ **AND ON 2026-08-26 THE CROWD RING TOOK ACT 3 AWAY — RE-AIMED AGAIN, NOT LOOSENED.**
# Five of the 24 were red because `F-055` gives a titan a standing place to walk to as soon as
# **one other titan** has also noticed the player, and `missions.ron: tutorial` brings its own
# husk to the fight. The whole diagnosis, its five controls and the positive control stand at
# ACT 3 below and in `docs/FINDINGS.md` FIND-177. Three coordinates moved; no assert was touched.
#
# ⚠️ **AND THAT RE-AIM PARKED ACT 3 TEN METRES FROM ACT 4 — RE-AIMED ONCE MORE, TO z = -90.**
# Green it made no difference; with `slash right 0.40` deleted from ACT 3 it made ACT 4 stop
# cutting too (final kills **1**, where the acts-are-independent answer is **2**). The two
# stands are now 50.99 m apart — `Senses::sight_range_m` is 45 m — and the fault-injection
# control is at ACT 3 with its arithmetic. `docs/FINDINGS.md` FIND-179.
#
# ⚠️ **THE MAP CHANGED UNDER THIS FILE ON 2026-08-12 AND IT WAS RE-AIMED, NOT LOOSENED.**
# `maps.ron: current` is `"ashgate"`; the graybox watchtower ACT 1 hooked and the lot ACT 4
# dropped into are both gone. Nine of 23 asserts were red. What changed here is **three
# coordinates, one yaw/pitch pair and two waits that cancel** — no assert was touched, and the
# four cut ticks (653/656, 774/777, 895/898) and the totals (23 asserts, 1200 ticks) are the
# same numbers the graybox run produced. What each move was and why is written at its act.
#   offscreen at tick 1100: the overlay reads `mission WON  kills 3/3`, and the PNG is
#   bit-identical over two runs —
#   sha256 5a4d359b28517882cd8c81ceab88f9d7577e54ca365c664c15f5344623488579
#
# 46.414 m/s is `scripts/f-001-hooks.txt`'s number to the last digit, out of a different file
# with a mission running and four titans in the world. The flight does not care.
#
# ⚠️ TWO RUNS, NOT ONE. `--screenshot` takes the exit over
# (`debug::screenshot::exit_when_written`) and writes `AppExit::Success` the moment the PNG is
# on disk — an `assert` after the shot tick never reaches the exit code. The VERDICT comes from
# the `--headless` run, the IMAGE from the `--offscreen` one.
#
# Every piece in here was proven separately before this file existed. This is the first run in
# which they are proven TOGETHER, which is the only thing this file is for.
#
# ACT 1 is `scripts/f-001-hooks.txt`'s flight, verbatim: aim at the watchtower, anchor, reel,
# land on the roof. It carries the one assert this session was told to check and never had:
# `assert speed > 25` — `docs/PLAN-GAME.md` §3.1 Risk 1, "the whole combat design assumes a
# player who moves at 30 m/s".
#
# ACTS 2-4 are `scripts/q030-reach.txt`'s cortex pass, three times, against three husks — with
# one change: the player is dropped from higher, and the husk is spawned 27 ticks INTO the
# fall instead of at its start. That keeps the husk's pose at the cut identical to the proven
# one (his `Recover`, lean 0) while the player arrives at the nape roughly nine metres per
# second faster.
#
#   fall of n ticks at gravity g, semi-implicit:  v = g n / 60,  drop = g n(n+1) / 7200
#   at g = 20:  n = 62 -> 20.7 m/s, 10.85 m  (q030) ;  n = 91 -> 30.3 m/s, 23.26 m
#   at g = 32:  n = 39 -> 20.8 m/s,  6.93 m  (q030) ;  n = 57 -> 30.4 m/s, 14.68 m
#
# so the drop height goes from f030's `21²/2g` to this act's `30²/2g` and the cut lands at the
# same nape — 18.5 m to 30.8 m at the old gravity, **14.19 m to 21.36 m at the current one.**
# **It is the two SPEEDS, 21 and 30, that are the constants here; both heights are functions of
# `gravity_m_s2` and are re-evaluated, never re-aimed.**
#
# The geometry of the pass is q030's and unchanged: the cortex sits at (x, 8.9, z+0.55) —
# `head_m / 2` behind the neck (`src/titan/rig.rs: cortex_in_head`) — the player stands
# 1.80 m to -X of the titan's axis (0.20 m of air between the two capsules) and looks at
# yaw 0, because the RIGHT blade lies on +X for yaw 0 (`src/blades/cut.rs: blade_segment`)
# and you cut what you fly PAST, not what you look at.
#
# Why the husk is spawned by the script and not waited for: `missions.ron: tutorial` puts its
# second wave at 90 s = 5 400 ticks and its third at 210 s. `spawn titan` writes exactly the
# same `SpawnTitan` message `mission::release_due_waves` writes (`src/mission/mod.rs`) — it is
# the mission's own spawner, addressed by hand. The mission's OWN first wave stands on the
# 24 m ring from tick 1 and is left alone: it is `husk#1`, and the three that get cut are
# `titan 2`, `3` and `4`.
#
# ⚠️ FINDING, not a decision of this file: `husk#1` reads `Idle` at tick 1100 — the one titan
# the MISSION put in the world never engaged over 18 seconds, while the player was on a roof
# and then 40 m away. The kills in this run are all off script-spawned bodies, so the run says
# nothing about whether the tutorial's own waves are a fight. -> docs/FINDINGS.md
#
# Nothing here injects a `TitanHit`. The script vocabulary has no verb for it on purpose. Every
# kill in this run comes out of `blades::cut`, off a real swing, at a real closing speed.

wait 1.5          # world, camera, meshes and the render pipelines come up (90 ticks)
key F3 0.05       # the overlay carries pos, tick, phase and kills into the PNG

# ***** THE INVOCATION GUARD — read this before you believe a red run of this file *****
#
# 🔴 **`--mission tutorial` is not optional here, and without it this file reports `10 of 23
# asserts failed` while NOTHING is wrong with the game.** Measured 2026-08-19 against one
# pinned binary: with the flag, 23 of 23 held and `MISSION WON at tick 899`; without it the
# identical run cut the same three napes at the same ticks (653/657, 774/778, 895/899) and
# failed exactly the ten `kills`/`phase` asserts, because with no mission loaded there is no
# tally to read and `Phase` reads 0. The flight, the map, the aiming and the roster were all
# innocent — the command line was short one flag.
#
# The assert below is that trap made loud: it fires at line 102 instead of at line 314, and its
# message is the diagnosis. `Phase == 2` is `Active` (0 Briefing 1 Deploying 2 Active 3 Won
# 4 Lost) and the tutorial is Active from tick 1.
assert phase == 2 # ⚠️ RUN THIS AS `--headless --mission tutorial`. A 0.000 here means the
                  # mission never loaded and every kills/phase assert below is meaningless.

# =========================================================================================
# ACT 1 — the flight. Hook a landmark, reel, and be moving faster than the design assumes.
# =========================================================================================
#
# ⚠️ **RE-AIMED 2026-08-12: the graybox watchtower does not exist in ashgate.** It stood at
# (24, 6, -40) with its roof at y = 12; the old `look 0 34` / `warp 24 0 -20` now shoots over
# an empty lot and every number in this act was red.
#
# **Ashgate's church is the replacement, and it is the same shape one storey taller.**
# `maps.ron` puts a 14 x 35 x 14 `anchorable: true` block at (45, 17.5, -22) — x 38..52,
# z -29..-15, **roof at y = 35**. Standing 14 m in front of its -Z face at 66.6 degrees up,
# the ray lands at y = 1.65 + 14 x tan 66.6 = 33.95, one metre under the roof line — the same
# aim the watchtower got (11.04 under a roof of 12), scaled to the taller face. Probed, one
# shot: `anchored at (45.00, 34.00, -29.00)`.
#
# `warp 45 0 -43` is clear ground, and that had to be checked rather than assumed: the market
# hall at x 38..64, z -15..-1 covers the stand the +Z face would have needed, and the HQ
# covers x -47..-15, z -13..13. Measured `height 0.050` on the tick after the warp.
#
# **The roof is 35 m and not 12 m, and that only makes the act's own claim safer**: the
# mission's own `husk#1` reaches the shoulder of a 10 m titan (`combat::strike`), so a perch
# that was 2 m out of reach is now 25 m out of reach.

look 180 66.6
warp 45 0 -43
wait 0.3          # the warp takes effect one tick later than it is written

assert height > -0.1    # standing on the ground, not sunk into it
assert height < 0.5
assert speed < 0.5      # and standing STILL — the baseline every number below is read against
mark game-aim

hook right 1.0          # the right trigger on its own: a rope that is not reeled is slack
wait 1.0
mark game-right-anchored
assert speed < 0.5

hook left 6.0           # and the left one — held, because the rope only exists while it is
wait 0.4
mark game-left-anchored

# 🔴 **THE VERB UNDER THIS KEY CHANGED ON 2026-08-25 AND NOT ONE ASSERT IN THIS ACT DID.**
# `vector.rope_force_model` is `Drive` since 2026-08-23, and `Drive` builds no `DistanceJoint`
# (`docs/FINDINGS.md` FIND-152) — so there was no length for the reel to shorten, `Ctrl` moved
# nobody, and this act reported `Speed 0.000 · Height 0.300`, four of the 24 asserts red, on a
# build that was pushed. What `Ctrl` means under the drive is now
# `player::locomotion::rope_winch`: **closing speed along the rope**, `vector.reel_speed_m_s`,
# no look gate, stopping at `vector.min_rope_m` (`docs/QUESTIONS.md` Q-050).
#
# Measured against one pinned binary, this act, the two models side by side:
#
#                        at `game-reeled`          at `game-on-the-roof`
#   Pendulum (length)    28.741 m/s · 15.521 m     0.000 m/s · 35.000 m
#   Drive    (winch)     26.695 m/s · 13.394 m     0.000 m/s · 35.000 m
#
# **The claim is the same claim**: the rope — and nothing else — takes a standing player past
# 25 m/s and 12 m up, and leaves him at rest on a 35 m roof. The gap in the first column is the
# amplification a constraint gives a reel and a winch cannot (`L_prev/L_new`,
# `shared::rope::rope_reel_in`); both readings clear the tripwire this act exists to trip, and
# `assert speed > 25` was written for 25 and is met by 26.695 with 1.7 m/s to spare.
# ⚠️ **That margin is thinner than the pendulum's 3.7 and it is the number to watch.** If this
# line goes red after a tuning change, `vector.reel_speed_m_s` is the key, not this file.
key Ctrl 5.0            # REEL_IN: under `Pendulum` the rope gets shorter and the player goes
                        # with it; under `Drive` the winch pulls him up the rope at 28 m/s
wait 0.6
mark game-reeled

# ***** docs/PLAN-GAME.md §3.1, Risk 1 — the tripwire *****
#
# ⚠️⚠️ **THESE TWO WERE RED FROM 2026-08-10 AND THEY ARE GREEN AGAIN — BUT READ WHY BEFORE
# YOU BELIEVE THE RISK IS RETIRED, BECAUSE IT IS NOT.**
#
#     assert speed > 25    46.414 m/s (overshoot bug) -> 19.344 RED -> **28.741** now
#     assert height > 12   13.064 m                   ->  9.881 RED -> **15.521** now
#
# The red was real. `B-004`'s take-up ratchet in `src/player/rope.rs` (2026-08-10) makes the
# rope follow the real distance DOWN and never up, so the player can no longer fly past his
# anchor; the 46.414 this file quoted for a day was that overshoot — a reel that ended at
# 54.18 m/s and then whipped (docs/QUESTIONS.md Q-032, docs/FINDINGS.md FIND-016). With the
# rope kept taut, the same shallow reel arrived at 19.344 m/s.
#
# ⚠️ **NOTHING IN THE ROPE CODE CHANGED BETWEEN 19.344 AND 28.741. THE ANCHOR DID.** The
# ratchet is still there. What moved is the geometry this act aims at: graybox reeled 34
# degrees up onto a 12 m roof, ashgate reels **66.6 degrees up onto a 35 m roof**, and a reel
# that is mostly vertical spends its shortening on height instead of on closing a horizontal
# gap. Same `reel_speed_m_s`, same ratchet, +9.4 m/s.
#
# So the honest sentence is **not** "the rope now does 30 m/s". It is: **the rope delivers 25+
# m/s when the anchor is steep, and 19 m/s when it is shallow, and this file only ever samples
# one anchor.** `assert speed > 25` is therefore a tripwire on THIS geometry and not on the
# vector gear — re-aiming it green is exactly the move that could retire Risk 1 without
# earning it. Risk 1 stays open (`docs/FINDINGS.md` FIND-065); the number a player actually
# gets over a district full of 8-11.5 m housing is unmeasured, and the housing is shallow.
#
# ACTS 2-4 below still cut at 30.00/30.33 m/s, and every one of those is a FALL.
assert speed > 25       # "the rest of this plan is designed for a player who moves at 30 m/s"
assert height > 12.0    # a jump reaches 1.056 m. Twelve metres is the rope and nothing else.
assert gas < 15000      # and the reel really was paid for out of the tank (`gas_tank` is
                        # 15000 since 2026-08-20, Q-046 — the old `< 100` rebased twice, not
                        # loosened: a zero-spend run reads exactly 15000.0 and fails this)

wait 2.0                # 1.2 -> 2.0 on 2026-08-12: the 35 m roof is 23 m further up than the
                        # watchtower's, so the reel is still running at the old mark. Measured
                        # 8.411 m/s at t=309 and 0.000 at t=357. The 0.8 s is given back at
                        # `wait 3.4` below, so every tick after `game-rope-released` — and the
                        # whole of ACTS 2-4 — is where it was.
mark game-on-the-roof
assert height > 11.5    # he ends up standing on the church roof: maps.ron, y = 35 — measured
                        # 35.000. The bracket is the watchtower's and is left as it was.
assert speed < 1.0
assert phase == 2       # Active — the mission has been running the whole time
assert kills == 0

# ⚠️ **LET GO OF THE ROPE BEFORE FIGHTING.** This is the one thing that only shows up when the
# pieces run together, and it cost this file its first two kills.
#
# `hook left 6.0` holds `KeyQ` for 360 ticks from t=173, so the rope is alive until t=533;
# `key Ctrl 5.0` reels until t=498. In the first version of this file ACTS 2 and 3 warped 55 m
# away while that `DistanceJoint` was still attached — the player was not falling, he was being
# yanked, and the blade never came near a nape. **Not one line of the log said so**: no assert
# fired, no hook message, nothing. Only the third cut landed, and it landed because it was the
# first one after t=533.
#
# So: the fight waits the rope out. The husk of the mission's own first wave is on the ground
# and cannot reach a roof at y = 35 anyway (`combat::strike`'s ceiling is the shoulder,
# 0.82 x 10 m).
wait 3.4                # past t=534: both rope keys released, the joint is gone.
                        # 4.2 -> 3.4 on 2026-08-12, the 0.8 s borrowed by `wait 2.0` above.
                        # The mark still lands at t=563, 29 ticks after the release.
mark game-rope-released
assert speed < 1.0      # standing on the roof, attached to nothing
assert height > 11.5

# =========================================================================================
# ACT 2 — the first husk. 0/3 -> 1/3.
# =========================================================================================

# 🔴 **THE THREE NUMBERS IN THIS ACT ARE DERIVED FROM `game.ron: gravity_m_s2`.**
# This is `scripts/f030-cortex.txt`'s pass with a head start, and the head start is expressed in
# SPEED, not in seconds — the act exists to cut at 30 m/s and not at f030's 21, so that the
# game's own flagship evidence is not all flown at one closing speed. Off that:
#
#     g       = -game.ron: gravity_m_s2 = 32.0
#     drop    = v² / (2 g) = 30² / 64 = 14.0625 m      (was 22.5 m at g = 20)
#     warp y  = cortex_height_m 8.90 - eye_height_m 1.60 + drop = 21.36   (was 30.8)
#     wait #1 = (v - 21) / g = 9 / 32 = 0.281          the head start, IN SECONDS OF FALL
#     wait #2 = 21 / g - 0.15          = 0.506          f030-cortex's own wait, unchanged
#
# **Control: put g = 20 back in and it prints `0.450` and `0.900`** — the two waits this file
# carried from 2026-08-19 to 2026-08-29, and a drop of 22.5 m against the 23.5 it carried. So
# `wait #1` really was "the 9 m/s", and it is 0.281 s at the new gravity and not 0.45.
warp -1.80 21.36 18.55  # 1.80 m to -X of the axis, on the plane of the cortex's centre
look 0 0
wait 0.281              # the head start: 9 m/s of extra fall BEFORE the husk exists — that is
spawn titan husk 0 0 18 # what puts the player at 30 m/s at a nape, where f030 is at 21
wait 0.506
slash right 0.40        # SLASH_RIGHT (`src/net/local.rs`): the right blade, on +X
wait 0.60
mark game-cut-1
assert kills == 1
assert phase == 2       # one kill of three does not win it

# =========================================================================================
# ACT 3 — the second husk. 1/3 -> 2/3.
# =========================================================================================

# ⚠️ **RE-AIMED 2026-08-26: this act used to stand in the crowd.** It was `warp 14.20 30.8
# 12.55` / `spawn titan husk 16 0 12`, and since `F-055` landed that is a place where the pass
# cannot work — for a reason that has nothing to do with the geometry and everything to do with
# who else is on the street.
#
# `missions.ron: tutorial` spawns **its own husk at `at_s: 0.0`**, and by tick 750 he has been
# walking after the player for twelve seconds. At `x 14.2, z 12.55` he is still inside his own
# `aggro_radius_m` (45 m, and `Senses::sight_range_m` is that same number) of the player and he
# has noticed him — so `perception::in_the_crowd` is true for **both** him and the husk this act
# just spawned, and `perception::claim_slots` hands each of them a ring bearing: `slot.of == 2`.
#
# That one number is the whole failure. `brain::walk` reads
#
#     in_position = slot.of <= 1 || slot.at_slot
#     pursuing    = Pursue && seen && (distance_m > attack_range_m || !in_position)
#
# and `target.distance_m` is **horizontal** (`perception.rs`, `Vec3::new(to.x, 0.0, to.z)`), so
# a player falling 1.88 m to the side of the nape is 1.88 m away, not 28 m. **Alone** the husk
# is therefore inside his 6 m reach with `in_position` true, `pursuing` is false and he is
# planted — which is exactly what ACTS 2 and 4 rely on and what `q030-reach.txt` proved. **In a
# crowd of two** his standing place moves to `player + ring_offset` — `min(9.0, 6.0 * 0.9)` =
# 5.4 m out — `at_slot` is false, and `pursuing` is true *even at 1.88 m*. He walks 2.7 m in the
# 0.90 s before the slash and the 0.55 m pass meets empty street. The symptom is the one ACT 4
# already documents for a bad position: **no `cut titan 3` line at all**.
#
# Measured, each control its own run (`--headless --mission tutorial`, 950 ticks):
#   husk speed_m_s 3.0 -> 0.0        -> the cut lands, 774/777   (he cannot walk)
#   crowd.arrive_m 1.0 -> 100.0      -> all three land            (at_slot always true)
#   crowd.ring_radius_m 9.0 -> 0.0   -> still red                 (the slot is still 1.88 m off)
#   tutorial wave at_s 0.0 removed   -> all three land            (no second crowd member)
#   ACT 4's lonely husk given a neighbour 6 m away -> **ACT 4 fails too**
# The last one is the positive control: crowding an act that works breaks it.
#
# **The game is right and this file was stale.** So the pass moves to the lonely southern
# stretch ACT 4 already proved clear — `x -15..15` is street the whole way — and it goes to the
# **far** end of it, at `z = -90`, for a second reason that is not the crowd at all.
#
# ⚠️ **RE-AIMED A SECOND TIME, 2026-08-26: `z = -40` COUPLED THIS ACT TO ACT 4.**
# The first re-aim put this husk at `(10, 0, -40)` — **ten metres from ACT 4's** at
# `(0, 0, -40)`. In the green run that is invisible, because a husk that dies leaves the crowd
# and is dissolved 60 ticks later. It is not invisible the moment ACT 3 goes wrong, and that is
# the case a regression test exists for. Measured, pinned binary, `slash right 0.40` deleted
# from THIS act and nothing else changed:
#
#   ACT 3 husk at (10, 0, -40)  ->  final kills **1**   — ACT 4 never cuts at all
#   ACT 3 husk at (10, 0, -90)  ->  final kills **2**   — `cut titan 4 Cortex` at 899
#
# A surviving ACT 3 husk at `z = -40` is 11.8 m from ACT 4's stand, so he is `detected` and
# inside `Senses::sight_range_m` (45 m, = husk `aggro_radius_m`), `in_the_crowd` is true for
# both him and ACT 4's husk, `slot.of == 2`, and ACT 4's husk walks off his nape exactly the way
# this act's used to. **One broken act silenced the next one**, and the file would have reported
# a single failure for two independent bugs.
#
# **The separation is 51.91 m and it is chosen against `sight_range_m`, not against taste:**
#
#   ACT 3 husk (10, 0, -90)  ->  ACT 4 stand (-1.80, -39.45):  dx 11.80, dz 50.55  =  51.91 m
#   ACT 3 stand (8.20, -89.45) -> ACT 4 stand (-1.80, -39.45): dx 10.00, dz 50.00  =  50.99 m
#
# 51.91 - 45 = **6.91 m of margin**, and the only thing that eats it is the husk walking: he is
# `speed_m_s` 3.0 and ACT 4 spends `wait #1 + wait #2` = **0.7875 s** between its warp (t=741)
# and its slash (t=788), so he can close at most **2.36 m**. **49.55 m at the slash tick, 4.55 m
# clear of the ring.** He cannot be half of ACT 4's crowd, whatever ACT 3 does.
# ⚠️ **That margin is a function of gravity**: the two waits are derived from it, so a heavier
# gravity shortens the pass and WIDENS this margin, and a lighter one narrows it. At `g = 20` it
# was 0.92 s / 2.75 m / 4.16 m of clearance. Re-evaluate it whenever `gravity_m_s2` moves — the
# number that must stay positive is `51.91 - 45 - speed_m_s x (wait #1 + wait #2)`.
#
# ⚠️ **And the terrace is why it is `z = -90` and not `z = +105`.** The obvious separation is
# northwards up the lane, and it does not work: `world::map` terraces the ground in 42 m cells,
# and the settled player height (`assert height`, one warp per stand, 3.0 s of fall each) reads
#
#   z = -90, -84, -60, -40 on x = 8.2  ->  **0.050**   (the y = 0 terrace, ACT 4's own)
#   z = +70  ->  1.500      z = +105 -> 3.000      z = +140 -> 3.000
#   z = -99  ->  30.000     (that is the top of the wall, `z <= -98`)
#
# A stand on a 1.5 m terrace is the same drop onto ground 1.5 m high with the husk's feet at 0,
# which is a different pass and a different tick. **The one direction that keeps the drop is
# south**, and south runs out at the wall — so `z = -90` is very nearly the far end of the
# usable ground, which is exactly why it is the right end of it to stand on.
#
# **The pass itself is untouched**: the 1.80 m of -X offset, the 0.55 m of cortex set-back, the
# drop and every wait are ACT 2's, and the cut lands on the same tick arithmetic — measured
# 2026-08-29, t=706 torso, t=709 cortex. **No assert was touched.** Two coordinates moved; the 24
# claims are the ones from 2026-08-19.
#
# The positive control still fires here, so the aloneness is a fact about this stand and not a
# rule that stopped applying: **giving this husk a neighbour at `(16, 0, -90)` deletes the
# `cut titan 3` line entirely** — same symptom, same mechanism, 6 m away.
# 🔴 **The standing lesson for every script in this repo:** a pass aimed at a fixed coordinate
# is only valid while its titan is **alone** in the player's crowd — and **two acts that each
# hold that on their own do not hold it together unless they are more than `sight_range_m`
# apart.** `assert titans` cannot see either half: it read 2.000 in the failing run and 2.000
# in the fixed one.

warp 8.20 21.36 -89.45
look 0 0
wait 0.281
spawn titan husk 10 0 -90
wait 0.506
slash right 0.40
wait 0.60
mark game-cut-2
assert kills == 2
assert phase == 2       # two of three, still not won

# =========================================================================================
# ACT 4 — the third husk, and the verdict.
# =========================================================================================
#
# ⚠️ **RE-AIMED 2026-08-12: this act used to drop into the HQ.** It was `warp -17.80 30.8
# 12.55` / `spawn titan husk -16 0 12`, and ashgate puts its headquarters at x -47..-15,
# z -13..13 with a roof at 11.5 m — so the husk spawned inside the building and the player
# landed on its roof instead of on the nape. The symptom was the one ACT 1 of this file
# predicts for a bad position: **no `cut titan 4` line at all**, and `assert kills == 3 —
# measured 2.000`.
#
# The pass is moved to open pavement on the main street (x -15..15 is street the whole way)
# at z = -40, which sits between the gantry stations at z = -17.5 and z = -52.5 and so has
# nothing overhead in the fall column either — and the column only got shorter when the drop
# went 30.8 → 21.36 m of start height on 2026-08-29. **The pass itself is untouched**: the
# 1.80 m of -X offset, the 0.55 m of cortex set-back, the derived drop and every wait are ACT
# 2's, and the cut lands on the same tick arithmetic — measured 2026-08-29, t=793 torso,
# t=796 cortex.

warp -1.80 21.36 -39.45
look 0 0
wait 0.281
spawn titan husk 0 0 -40
wait 0.506
slash right 0.40
wait 0.60
mark game-cut-3

# ***** the whole point of the session *****
assert kills == 3
assert phase == 3       # WON (mission::MissionPhase::code: 0 Briefing 1 Deploying 2 Active
                        # 3 Won 4 Lost) — decided by mission::decide off three cortex cuts
assert tick < 19800     # and NOT by the clock: the tutorial's deadline is 330 s
assert health > 0       # nor by going down: this is the winning half, not the losing one

# The picture. The player has landed and stands still; the camera turns off the empty sky and
# back over the field he just cleared. A resting moment on purpose — `--offscreen` is only
# bit-reproducible where the state changes slowly (`scripts/f-001-hooks.txt`).
#
# yaw -55: `forward = (-sin yaw, 0, -cos yaw)`, so from (-1.8, ., -39.45) — ACT 4's new stand
# on the main street — that points back up the boulevard at the district and the wall behind
# it. The three cut husks have dissolved by then (`death_s` 1.0 s), and what the PNG carries is
# the overlay's `mission WON  kills 3/3`, plus the whole HUD: gas bar, five blade pips, a full
# health bar, crosshair.
# ⚠️ The framing changed with ACT 4's move and `docs/images/f071-won.png` was NOT retaken here
# — that is a picture job, and this file's verdict is the headless run.
wait 1.5
look -55 2
mark game-won
assert kills == 3       # still 3/3 at the shot tick, and still won
assert phase == 3
wait 3
