// traits.ron — traits and lineage passives (relics, echoes).
//
// ⚠️ EMPTY ON PURPOSE. Progression is a meta system and is not started as long as the
// Vector Gear gate is not standing (bible 6.1, docs/ROADMAP.md). The file exists so that
// data/ loads it and the structure is fixed — not so that somebody starts here.
(
    entries: {},
)
