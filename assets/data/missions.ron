// missions.ron — mission templates: objectives, phases, spawn waves, reward.
// A new mission is file work, not Rust (init.md §4).
//
// STATUS: a single template, so that `--mission tutorial` has something to load.
// A mission arc lasts 5–7 min (bible 5, change 10).
(
    templates: {
        "tutorial": (
            name: "First Ride",
            map: "ashgate",
            target_duration_s: 330.0,
            waves: [
                (at_s:   0.0, kind: "husk",     count: 1),
                (at_s:  90.0, kind: "husk",     count: 2),
                (at_s: 210.0, kind: "scuttler", count: 1),
            ],
        ),
    },
)
