# docs — the index: one line per file

Updated: 2026-08-09 · Stage: 🟨

**Every file under `docs/` stands here.** Whatever is not here is an orphan file and gets
linked or deleted — there is nothing in between (`prompts/init.md` §10). `tools/norms.py`
checks that.

## Where to start

| Question | File |
|---|---|
| *Where do we actually stand?* | [`STATUS.md`](STATUS.md) — the four stages, generated |
| *What is next to do?* | [`TODO.md`](TODO.md) — open work in buildable order, generated |
| *How does this project work?* | [`../CLAUDE.md`](../CLAUDE.md) — the index of the rules, under 150 lines |
| *Which machine am I sitting on?* | [`environment.md`](environment.md) — **the first question of every session** |

## The maintained files

| File | Content |
|---|---|
| [`architecture.md`](architecture.md) | domains, plugin order, the **allow list** of dependencies, the authority table (who writes which field), the Roblox→Bevy translations |
| [`conventions.md`](conventions.md) | axes, units, looking direction, 1 stud = 0.28 m, the binding terms, the three signal colors, all the naming norms |
| [`models.md`](models.md) | the model chain, the anchor empties, the three glTF traps — and **the instructions for the user** on how to swap a model |
| [`multiplayer.md`](multiplayer.md) | the plan that does not get built yet: eight rules, the authority model, the seam `src/net/` |
| [`environment.md`](environment.md) | the two machines, measured: what works, what does not, and what follows from that |
| [`BUGS.md`](BUGS.md) | every bug with repro, evidence, expectation, cause — and the fix doctrine (red test first) |
| [`HANDOVER.md`](HANDOVER.md) | **read this first when picking the project up** — where the last session stopped, what is measured, what comes next |
| [`QUESTIONS.md`](QUESTIONS.md) | decisions that do not belong to Claude. Eleven open ones, each with the `ASSUMPTION:` the work continues under |
| [`FINDINGS.md`](FINDINGS.md) | mistakes that turned up *along the way* — with a measurement, **not quietly fixed on the side** |
| [`ACCEPTANCE.md`](ACCEPTANCE.md) | what the user should look at, so that something can become ✅ |
| [`ROADMAP.md`](ROADMAP.md) | what deliberately comes later, and why — the gate rule first |

## The generated files — **never edit by hand**

Hand work in them is lost on the next run. Each one carries its generator in its head.

| File | generated by | out of |
|---|---|---|
| [`features.ron`](features.ron) | `tools/features.py` | `gameplay/features.xlsx` — 245 rows (194 `F-` + 51 `T-`). Stage, evidence and note live **here**; they are carried over on a re-run |
| [`STATUS.md`](STATUS.md) | `tools/features.py` | `features.ron` |
| [`TODO.md`](TODO.md) | `tools/features.py` | `features.ron` |
| [`backlog/README.md`](backlog/README.md) | `tools/features.py` | sheet `00_Anleitung` + the row counts of all sheets |
| [`backlog/gameplay.ron`](backlog/gameplay.ron) | `tools/features.py` | sheet `01_Spielfunktionen` (194) |
| [`backlog/models.ron`](backlog/models.ron) | " | sheet `02_3D-Assets` (100) |
| [`backlog/animations.ron`](backlog/animations.ron) | " | sheet `03_Animationen` (100) |
| [`backlog/textures.ron`](backlog/textures.ron) | " | sheet `04_Texturen` (28) |
| [`backlog/vfx.ron`](backlog/vfx.ron) | " | sheet `05_VFX` (39) |
| [`backlog/audio.ron`](backlog/audio.ron) | " | sheet `06_Audio` (118) |
| [`backlog/ui.ron`](backlog/ui.ron) | " | sheet `07_UI-Screens` (45) |
| [`backlog/maps.ron`](backlog/maps.ron) | " | sheet `08_Maps` (12) |
| [`backlog/tech.ron`](backlog/tech.ron) | " | sheet `09_Tech-Backlog` (51) |
| [`backlog/naming.ron`](backlog/naming.ron) | " | sheet `10_Namensschema` (40) — **binding** |

Sheet `11_Zusammenfassung` is **not** transferred: it is computed. It serves as an
**independent counter-check** — its `COUNTA` formulas confirm our extraction row by row
([`FINDINGS.md`](FINDINGS.md) FIND-003).

## The case histories — the most valuable folder in the project

| File | what cost time |
|---|---|
| [`lessons/bevy.md`](lessons/bevy.md) | setting Bevy up, and the engine traps: API drift between minor versions, tuple limits, deferred commands, `cargo run` instead of the bare binary |
| [`lessons/performance.md`](lessons/performance.md) | the spatial index, "nothing per frame, everything per second", vsync as a measurement ceiling, shadows |
| [`lessons/workflow.md`](lessons/workflow.md) | the tools first: flags, the `--script` driver, the F3 overlay, screenshots |
| [`lessons/environment.md`](lessons/environment.md) | two machines, a full disk, a broken incremental cache, parallel sessions |
| [`lessons/supervision.md`](lessons/supervision.md) | parallel and scientific: the supervisor, the ways of cutting the work, the evidence obligation, the report format |

## The runs in `scripts/`

`scripts/` **plays** the game, `tools/` **builds** things — that separation does not get
blurred (`prompts/init.md` §5). A run with `assert` in it is a test, not a demo video.

| File | what it drives |
|---|---|
| [`../scripts/t007-first-run.txt`](../scripts/t007-first-run.txt) | the smoke test: does the game run, does nobody fall through the ground, does a jump have height |
| [`../scripts/t019-latency.txt`](../scripts/t019-latency.txt) | the same run at 200 ms of simulated latency (`--lag 200`, bible T-019) |

```bash
cargo run -- --headless --script scripts/t007-first-run.txt --ticks 600
cargo run -- --headless --lag 200 --script scripts/t019-latency.txt --ticks 900
```

## Empty so far, but planned

| Folder | what for |
|---|---|
| `gameplay/` | the design per topic (`<topic>.md`), plus `references.md` and `images/`. It fills up as things are carried over out of the inbox |
| `images/` | screenshots as evidence: `<f-id>-<short>.png`. **On machine A nothing appears here** |

## A named deviation: `docs/` does not mirror `src/` file by file yet

`prompts/init.md` §5 asks for one doc file per source file. As long as a domain consists of
nothing but an empty `mod.rs`, that would be a folder full of files without content — exactly
the orphans §10 forbids. **Until then [`architecture.md`](architecture.md) carries the
description of every domain in one line.** Whoever writes the first real logic into a domain
creates `docs/<domain>.md` and enters it here — **in the same commit**.
