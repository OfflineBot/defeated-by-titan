# avian-blockers — measurement report, 2026-08-09

Updated: 2026-08-09 · Stage: 🟧 (command output, reproducible)

**Aufgabe** — Die drei von der Gegenprobe gemeldeten Blocker (B1 `finish()`, B2 Zielstrahl, B3 `CollisionEventsEnabled`) einzeln am installierten Quelltext belegen **und** messen; Urteil bestaetigt/widerlegt/teilweise plus kleinste Reparatur. Nur Scratchpad beschreiben.

**Getan** — Alle Behauptungen an `avian3d-0.7.0/src` und `bevy_app|bevy_ecs-0.19.0/src` nachgeschlagen (alle von der Gegenprobe genannten Zeilennummern stimmen). Wegwerf-Cargo-Paket `/tmp/claude-1000/-home-offlinebot-Documents-defeated-by-titan/3285eda4-fff6-4374-861e-3da38ba9f775/scratchpad/blocker/` gebaut, das per `path`-Abhaengigkeit die **echte** App aus `src/lib.rs` baut und sich `target/` mit dem Projekt teilt (kein zweites target). Bericht: `/tmp/claude-1000/-home-offlinebot-Documents-defeated-by-titan/3285eda4-fff6-4374-861e-3da38ba9f775/scratchpad/blocker.md`. Im Projektbaum nichts geaendert.

**Urteile**

- **B1 — teilweise.** Mechanik bestaetigt und gemessen: ohne `finish()` paniert die echte App beim ersten Physiktick, `EXIT=101`, „Parameter … failed validation: Resource does not exist" (`bevy_ecs-0.19.0/src/error/handler.rs:130`); mit `finish()+cleanup()` laeuft sie und die Kugel faellt. **Aber: „alle 95 Tests panieren" ist heute falsch** — `grep -rn avian src/` ist leer, `app()` registriert kein avian-Plugin, `cargo test` steht bei **116 gruen**. B1 ist der Blocker fuer den Commit, der avian in `app()` haengt, nicht fuer jetzt. Reparatur: zwei Zeilen `app.finish(); app.cleanup();` am Ende von `pub fn app()` (idempotent, `schedule_runner.rs:77-86` prueft `PluginsState::Cleaned`). **Gefaehrlicher Nebenbefund: ein einzelnes `app.update()` paniert nicht — es fuehrt gar keinen Physikschritt aus.**
- **B2 — bestaetigt, und durch die Zusatzfrage vollstaendig aufgeloest.** Gemessen mit Kind-Collider: `excluded([spieler])` → Treffer auf die eigene Kapsel bei **0,00 m**, unveraendert gegenueber `default`. `excluded(RigidBodyColliders)` und `cast_ray_predicate` → Wand bei 9,75 m. **Die beste Reparatur ist aber `Collider::capsule_endpoints(r, (0,r,0), (0,h-r,0))`** an derselben Entity (`A/collision/collider/parry/mod.rs:800-802`): AABB −0,005…1,805, `ComputedCenterOfMass` = 0,9 automatisch, Ruhelage y ≈ 0, und `excluded([spieler])` **wirkt** (9,75 m). Kind-Entity entfaellt, B2 entfaellt. `ColliderTransform` ist kein Nutzer-Versatz („updated automatically, you shouldn't modify it manually").
- **B3 — bestaetigt, beide Haelften.** Gezaehlte `CollisionStart` ueber 240 Ticks: am Koerper mit Kind-Collider **0**, nachtraeglich eingefuegt **0**, im Spawn-Bundle der Collider-Entity **2**, am Kind-Collider **1**, `CollisionEventsEnabled` + Collider neu eingefuegt **2**. Alle sechs mit identischer Ruhelage — **der Fehler ist stumm**. Ursache: das Flag wird in `add_to_tree_on` (`A/collider_tree/update.rs:404-455`, ausgeloest nur von `:124` `Insert` auf `(Collider, ColliderOf)`) ins BVH-Proxy eingebacken; die Fallliste `:107-121` kennt kein `CollisionEventsEnabled`. **Fuer F-013 braucht man die Nachrichten gar nicht:** `Collisions` (`A/collision/contact_types/system_param.rs:53-58`) plus `ContactPoint::normal_speed` (`mod.rs:626-635`, geschrieben in der Narrow Phase `narrow_phase/system_param.rs:751-752`, unabhaengig von Ereignissen). Gemessen ohne jedes Ereignis-Component: Kontakt in Tick 31, `normal_speed = -9,67 m/s` gegen theoretische 10,0 m/s, Impuls 5,27.

**Beleg** — `/tmp/claude-1000/-home-offlinebot-Documents-defeated-by-titan/3285eda4-fff6-4374-861e-3da38ba9f775/scratchpad/blocker.md` (Zeilenangaben, Kommandoausgaben, Reparaturen mit fertiger Rust-Zeile) · Rohausgaben `scratchpad/blocker/b1p-ohne.txt`, `b1p-mit.txt`, `b2.txt`, `b3.txt` · Messprogramm `scratchpad/blocker/src/main.rs`.

**Stufe** — 🟧 fuer alle drei: jede Aussage hat eine Kommandoausgabe **und** eine Quelltextstelle; kein Bild, weil kopflos gemessen (Zahlen statt Pixel).

**Offen** — (1) Ob `finish()+cleanup()` in `app()` mit `--headless`+`ScheduleRunnerPlugin` **und** mit Fenster gleichermassen sauber laeuft, habe ich nur fuer `update()` gemessen, nicht fuer `run()`. (2) Die Kosten von `cast_ray_predicate` gegen `RigidBodyColliders`-Filter habe ich **nicht** in ms gemessen, nur begruendet. (3) `target/` ist durch meinen Bau von 32 G auf 37 G gewachsen (115 G frei) — die Wegwerf-Artefakte liegen unter dem geteilten `target/`, `cargo clean -p blocker` raeumt nur den Bin.

**Funde**
1. **avian 0.7.0 hat einen fertigen Sweep-Bewegungsloeser**: `MoveAndSlide` als `SystemParam` (`A/character_controller/move_and_slide.rs:69-87`, `:485-495`, im Prelude ueber `A/lib.rs:565`) mit Entpenetrierung und `skin_width`. Das ist genau der Schiedsrichter zwischen Seil und Geometrie, dessen Fehlen der Fund dieser Runde beklagt — er geht per Konstruktion nicht durch eine 0,5-m-Wand. **Empfehlung: „Seil als DistanceJoint" und „Bewegung ueber move_and_slide" gehoeren in eine Runde, nicht in zwei.**
2. `cargo test` steht bei **116** gruenen Tests (67 lib · 39 data · 3 domaenen · 7 mehrspieler), nicht 95 — eine andere Sitzung hat nachgelegt (waehrend meiner Messung kamen die Commits `4d7ea88` und `bb521cd` dazu).
3. `examples/probe_avian.rs:90,184,888` ruft `app.finish()` bereits von Hand — wer die Datei als Vorlage nimmt, uebernimmt die B1-Reparatur unbewusst und merkt beim Umzug in die echte App nicht, warum es dort knallt.