export const meta = {
  name: 'dbt-stage3-vector-gear',
  description: 'Stage 3: the Vector Gear — hooks, aiming, pendulum, reel-in, gas boost',
  phases: [
    { title: 'Seam', detail: 'Register avian, put the player into the physics world' },
    { title: 'Gear', detail: 'F-001 hooks, F-002 aiming, F-018 gas, F-007 boost — in parallel' },
    { title: 'Rope', detail: 'F-004 pendulum and F-005 reel-in on top of the hooks' },
    { title: 'Proof', detail: 'One agent attacks all of it, takes the pictures, judges the stages' },
  ],
}

const W = '/home/offlinebot/Documents/defeated-by-titan'
const S = '/tmp/claude-1000/-home-offlinebot-Documents-defeated-by-titan/3285eda4-fff6-4374-861e-3da38ba9f775/scratchpad'

const BASE = [
  'PROJECT: "Defeated by Titan" — a 3D action game about fighting Titans. Bevy 0.19 +',
  'avian3d 0.7.0 (Rust). Root: ' + W + '. Machine B: offlinebot, 16 threads, RTX 3080,',
  'cargo in /usr/bin. **The whole repository is English since commit fc2dc45** — code,',
  'comments, docs, RON keys, log output. Keep it that way; no German anywhere.',
  '',
  'THE CORE OF THE GAME is the Vector Gear: two hooks, two gas tanks, two blades. You are',
  'building it. The design bible gates everything else behind it: no meta system gets built',
  'until the movement feels convincing.',
  '',
  'READ FIRST (targeted, not everything):',
  '  ' + W + '/CLAUDE.md — the six rules, file ownership, autonomous operation',
  '  ' + W + '/docs/conventions.md — axes, units, naming, test names',
  '  ' + W + '/docs/architecture.md — domains and the allow list',
  '  ' + W + '/docs/ACCEPTANCE.md — what counts as evidence, and the proven screenshot path',
  '  the header of the file you own — your commission is already written there',
  '',
  '=== THE ARCHITECTURE IS DECIDED AND MEASURED. Do not re-litigate it. ===',
  'Three measurement rounds produced this; every line carries the number that decided it:',
  '  ROPE:      avian `DistanceJoint` with `limits = (0, L)`. It only pulls, never pushes',
  '             (joints/mod.rs:329-343 corrects only when distance > max). Reeling in with',
  '             the joint gives 58.23 m/s from v0 20 (angular momentum preserved); a',
  '             hand-written clamp gives exactly 20.000 — it eats the reel-in, which IS the',
  '             feeling. Two taut ropes: 0.23 mm error against 24.9 mm for the hand-written one.',
  '  REFEREE:   none. With the speed clamp, 24 substeps and per-substep reeling, the worst',
  '             wall penetration across 18 measured cases is -0.0043 m of -0.01 allowed.',
  '             **But: every world collider MUST carry a `RigidBody` (`Static` is enough)** —',
  '             otherwise a referee added later is blind to it (move_and_slide.rs:82 filters',
  '             on `With<ColliderOf>`), and that would be rework on every map row.',
  '  SUBSTEPS:  24. The only measured value that holds the swing loss (4.26 %/s vs 8.97 %/s',
  '             at 6) AND the wall. Costs 0.72 ms/tick with 20 players in a 401-body city,',
  '             budget 4 ms. `SubstepCount` is a Resource, default 6 — the value belongs in RON.',
  '  REEL-IN:   shorten `limits.max` **per substep**, never per tick. Per tick injects',
  '             `rate x SubstepCount` = 677 m/s. Per substep: 130 m/s. With',
  '             `MaxLinearSpeed(max_speed_m_s)` on top: 75.44 m/s. Both screws are needed.',
  '',
  'THREE BLOCKERS, measured, with their fix — do not rediscover them:',
  '  B1 `App::update()` does NOT call `Plugin::finish()` (bevy_app app.rs:165-171; finish is',
  '     at :268 and only `run()` calls it). avian creates `SolverDiagnostics` in',
  '     `Plugin::finish` and several systems take it as a non-optional `ResMut`. Without it',
  '     EVERY test panics with "Resource does not exist" the moment avian is registered.',
  '     Fix: `app.finish(); app.cleanup();` at the end of `pub fn app()`. Idempotent.',
  '  B2 A ray hits the player\'s own collider, and `with_excluded_entities([player])` does',
  '     NOT help, because the filter matches the COLLIDER entity, not the body.',
  '     Best fix, measured: `Collider::capsule_endpoints(r, (0,r,0), (0,h-r,0))` on the SAME',
  '     entity (parry/mod.rs:800-802). No child entity, correct center of mass at h/2,',
  '     resting position y=0, and the exclusion works. Do not use a child collider.',
  '  B3 `CollisionEventsEnabled` on the body does nothing; it is baked into the BVH proxy',
  '     when the collider spawns. For impact data you do not need events at all: `Collisions`',
  '     (contact_types/system_param.rs:53-58) plus `ContactPoint::normal_speed` (mod.rs:626-635).',
  '',
  'AND: players need `SleepingDisabled` or a tuned `SleepThreshold` — default is 0.15 m/s',
  'after 0.5 s (rigid_body/sleeping.rs:103-107). A player hanging still on a rope falls asleep.',
  '',
  'HARD RULES:',
  '- Numbers belong in RON, never in Rust. No `serde(default)` for game values.',
  '- No `.single()` on players. Player state never a `Resource`. No `Local<T>` in a simulation',
  '  system. Simulation in `FixedUpdate`. Stable ids instead of `Entity` in messages.',
  '- ONE field, ONE writer — name it. avian counts as a writer where it writes',
  '  (Position, Rotation, LinearVelocity, Transform via Writeback).',
  '- Never guess an API. Bevy 0.19 and avian 0.7.0 are newer than your knowledge. Check the',
  '  installed source: ~/.cargo/registry/src/*/bevy_*-0.19.0/src/ and */avian3d-0.7.0/src/,',
  '  and cite file:line.',
  '- Foreign territory goes into your report as a finding, never silently fixed. Other agents',
  '  are working in this tree right now.',
  '- No tool or author traces in any file. You do NOT commit.',
  '',
  'THE SCREENSHOT PATH IS PROVEN — use it for your picture:',
  '  cargo run -- --offscreen --script scripts/<name>.txt --ticks <n> --screenshot docs/images/<f-id>-<short>.png',
  'It writes a real PNG without a window, bit-identical across runs. `--headless` gives NO',
  'image (exit 1). **Look at your picture** (Read displays PNG) and describe what is on it.',
  'A picture you have not looked at is not evidence.',
  '',
  'REPORT FORMAT (binding): Task · Done · Evidence · Stage · Open · Findings.',
  'Stage is ⬜ / 🟨 / 🟧 — and 🟧 needs all three: a picture, a measured number, and a test',
  'you have SEEN go red. Doubt moves the stage down, not up.',
].join('\n')

phase('Seam')

const SEAM = [
  BASE, '',
  '=== YOUR COMMISSION: THE SEAM. Everything else waits for you. ===',
  '',
  'YOU OWN: ' + W + '/src/lib.rs · src/player/mod.rs · src/player/integrator.rs ·',
  'src/player/locomotion.rs · src/world/mod.rs · src/world/map.rs · assets/data/game.ron ·',
  'tests/player.rs (new) · Cargo.toml',
  'YOU DO NOT TOUCH: src/vector/*, src/render/*, src/debug/*, src/net/*, docs/ (except as',
  'noted), examples/. Four agents start on src/vector/ the moment you are done.',
  '',
  'TASK: avian is a dependency and measured, but `PhysicsPlugins` is NOT registered anywhere',
  '(`grep -rn avian src/` is empty). The game still moves the player with a hand-written',
  '`translation += velocity * dt` and a hard-coded ground plane at y=0. Replace that with the',
  'physics world.',
  '',
  '1. **Register avian** in `src/lib.rs`: `PhysicsPlugins::new(FixedUpdate)`, and put ALL FIVE',
  '   `PhysicsSystems` stages together into the simulation set where the physics belongs',
  '   (read `src/shared/schedule.rs` for the existing set chain and pick the right one).',
  '   Measured: putting only one of the five in there does NOT panic — it silently misplaces',
  '   `Prepare` and `Writeback`. All five or none.',
  '   Add `app.finish(); app.cleanup();` (blocker B1).',
  '2. **Gravity and substeps come from RON**, not from avian defaults. avian has a `Gravity`',
  '   resource (default -9.81); `game.ron` says `gravity_m_s2: -20.0` and that is the truth.',
  '   Add `substeps: 24` to `game.ron` next to `simulation_hz` and feed `SubstepCount` from it.',
  '   Guard both in `tests/data.rs`... which is NOT yours — report the needed guard instead.',
  '3. **The player becomes a physics body**: `RigidBody::Dynamic`,',
  '   `Collider::capsule_endpoints(...)` on the SAME entity (blocker B2, exact call above),',
  '   `MaxLinearSpeed(max_speed_m_s)` from RON (this IS F-012, measured to hold at exactly',
  '   75.0000), `SleepingDisabled`, and friction/restitution from RON — avian defaults are',
  '   0.5 friction / 0.0 restitution, and a measurement showed 0.65 combined friction eats',
  '   75 % of the speed per second when a swinging player grazes a wall in a 7 m alley.',
  '   Pick honest starting values, mark them UNTUNED in the file like the others, and say in',
  '   your report which number you invented.',
  '4. **The world colliders get `RigidBody::Static`** — see the architecture note. Check',
  '   whether `world/map.rs` already sets it; if yes, say so, do not touch it.',
  '5. **The old movement code goes.** `player::move_player` (or whatever it is called now)',
  '   integrated by hand and clamped to a ground plane at y=0 with `ground_y = 0.0` hard-coded.',
  '   Ground contact now comes from the collider. Walking and jumping stay — but they write',
  '   through avian now. Read the measured note: acceleration, not force, so mass stays',
  '   irrelevant (`Forces` query data, `apply_linear_acceleration` "ignoring mass").',
  '   For ground locomotion a direct horizontal velocity write is the honest way — put it in',
  '   ONE system, name it in the authority table, and say so in your report.',
  '',
  'ACCEPTANCE — real output in your report:',
  '  cargo check --all-targets 2>&1 | grep "^error"   -> EMPTY',
  '  cargo test 2>&1 | grep "test result"             -> 0 failed. The count WILL change',
  '     (you delete tests of the old movement, you add new ones). Say which you removed and why.',
  '  cargo run -- --headless --script scripts/t007-first-run.txt --ticks 600',
  '     -> this run asserts height and speed. Under real physics the numbers WILL differ.',
  '     If an assert now fails, that is a RESULT: report it with both numbers and adjust the',
  '     script only if the new number is physically correct. Never adjust an assert to make',
  '     it pass — say what changed and why it is right.',
  '  A picture: docs/images/t007-physics.png showing the player standing on the ground in the',
  '     city, not sunk into it and not floating. Look at it.',
  '  A test you have seen go red — e.g. remove `MaxLinearSpeed` and watch the clamp test fail.',
].join('\n')

const seam = await agent(SEAM, { label: 'stage3:seam', phase: 'Seam' })

log('Seam done — four gear commissions in parallel')

phase('Gear')

const GEAR = [
  {
    id: 'F-001', label: 'hooks', file: 'src/vector/hook.rs',
    task: [
      'F-001 DOUBLE HOOK — two independently controllable grappling hooks (left/right), each',
      'with its own state: idle, flying, anchored, retracting. Acceptance from features.ron:',
      '"both hooks can be set and released independently; states are visible in the HUD".',
      '',
      'The buttons already exist: `Buttons::HOOK_LEFT` / `HOOK_RIGHT` in src/shared/intent.rs.',
      'Edge detection must NOT use `Local<T>` — a Local is shared across all players and',
      'invisible in a snapshot. Use a component on the player for the previous buttons.',
      'The hook flies at `hook_flight_m_s` and retracts at `hook_retract_m_s` (both in RON).',
      'It anchors where the aim point says — read the `AimPoint` component that F-002 writes;',
      'if it is not there yet, define the dependency and use what the interface promises.',
      'A hook stores WHERE it is anchored as a world position plus the stable id of the body',
      'it hangs on — never an `Entity` (it goes over the wire one day).',
      '',
      'CRITERIA: hook flight time matches `distance / hook_flight_m_s * 60` within one tick',
      '(this goes red the moment someone hard-codes the speed) · both hooks anchor and release',
      'independently, proven with a two-hook test · a hook whose carrier disappears releases.',
    ].join('\n'),
  },
  {
    id: 'F-002', label: 'aiming', file: 'src/vector/aim.rs',
    task: [
      'F-002 FREE AIMING BY RAYCAST — a ray from the eye along the look direction, range',
      '`hook_range_m` (90 m, the user set it). The hit point is checked against a valid anchor',
      'surface. Acceptance: "every tagged surface is reachable by free aiming, even where no',
      'anchor point was placed".',
      '',
      'USE avian\'s `SpatialQuery` — measured at 0.21 us for a 112 m ray against 4000 blocks.',
      '**Cast with mask = ALL and check the surface AFTER the hit**, never filter in the ray:',
      'measured, a filtered ray travels 19.85 m THROUGH an untagged wall to reach a roof at',
      '9.85 m. features.ron F-023 forbids exactly that ("line-of-sight check prevents hooking',
      'through walls"). First hit, then ask whether it is taggable.',
      'Blocker B2 applies: exclude the player\'s own collider properly (see the base text).',
      'The eye position is `translation + Y*eye_height_m`, the direction is `Intent::look()`.',
      '',
      'CRITERIA: the hit point matches the FULL known coordinate, not just "distance to the',
      'plane" — a ray origin displaced by 1.65 m in Y still lands on the wall plane and would',
      'pass a plane test while pointing at the wrong height · an untagged wall IN FRONT of a',
      'taggable roof yields "not hookable", not the roof · beyond `hook_range_m` there is no hit.',
    ].join('\n'),
  },
  {
    id: 'F-018', label: 'gas', file: 'src/vector/gas.rs',
    task: [
      'F-018 GAS RESOURCE — the tank, and who is allowed to spend from it. `Gas` already',
      'exists as a component (src/shared/state.rs) with an atomic `spend` that either takes',
      'the full amount or nothing.',
      '',
      'THE PROBLEM you are solving: boost (F-007) and reel-in (F-005) both consume gas in the',
      'same tick. With a nearly empty tank, whoever runs first wins — and that is a coin flip',
      'at 60 Hz, plus a desync in multiplayer. Build a BOOKING system: one system debits once',
      'per tick and publishes a component saying who got fuel. Everyone else reads that',
      'component and contributes zero if they got nothing.',
      'The ORDER when the tank is short is a GAME VALUE, not an `if` in the code — it belongs',
      'in RON (`gas_priority` already exists in game.ron, read it).',
      '',
      'CRITERIA: consumption per second matches the RON values exactly (boost 18/s + reel 6/s',
      '= 24/s, measured against the tick count — goes red if someone multiplies by dt twice) ·',
      'with a tank that only covers one consumer, exactly one gets it and the RON order decides ·',
      'no half boost on an empty tank · `--sandbox` (unlimited gas) never runs out.',
    ].join('\n'),
  },
  {
    id: 'F-007', label: 'boost', file: 'src/vector/boost.rs',
    task: [
      'F-007 GAS BOOST — an impulse along the look direction while the button is held,',
      'consuming gas continuously. Acceptance: "boost produces noticeable acceleration; the',
      'gas bar drops visibly".',
      '',
      'MEASURED AND DECIDED: this is an ACCELERATION, not a force — `Forces` query data,',
      '`apply_linear_acceleration` ("ignoring mass", query_data.rs:482). That keeps mass out',
      'of the game values entirely, which matters because the capsule weighs 0.6 kg at default',
      'density and every force number would be meaningless.',
      'avian accumulates into `VelocityIntegrationData.linear_increment` and clears it itself',
      '(integrator/mod.rs:316-327) — do NOT reset anything by hand.',
      'Gas comes from the booking component that F-018 publishes. Do not call `Gas::spend`',
      'yourself — that would be a second writer.',
      '',
      'CRITERIA: with the clamp removed, velocity after 2 s equals `boost_m_s2 * 2` within 1 %',
      '(measured: 67.4332 m/s at 34 m/s2 — goes red if someone applies dt twice or uses force) ·',
      'with the clamp, speed stops at exactly `max_speed_m_s` · a held button with an empty',
      'tank produces exactly zero acceleration, not a fraction.',
    ].join('\n'),
  },
]

const gear = await parallel(GEAR.map((g) => () => agent([
  BASE, '',
  '=== YOUR COMMISSION: ' + g.id + ' — ' + g.label.toUpperCase() + ' ===',
  '',
  'YOU OWN: ' + W + '/' + g.file + ' and ' + W + '/tests/vector_' + g.label + '.rs (new)',
  'and ' + W + '/scripts/' + g.id.toLowerCase() + '-' + g.label + '.txt (new)',
  'and ' + W + '/docs/images/' + g.id.toLowerCase() + '-' + g.label + '.png (new).',
  'YOU DO NOT TOUCH anything else — three other agents are in src/vector/ right now, and',
  'src/vector/mod.rs, src/lib.rs, assets/data/*.ron belong to the main head.',
  'The stub file you own already carries its commission in the header — read it first.',
  'If you need a system registered in src/vector/mod.rs and it is not there: REPORT it,',
  'do not edit the file. If you need a RON value that does not exist: REPORT it with path,',
  'type and unit, and use the closest existing one meanwhile, marked in a comment.',
  '',
  g.task,
  '',
  'THE SEAM AGENT JUST FINISHED. Here is what it did — read it, your code sits on top:',
  seam,
  '',
  'ACCEPTANCE: cargo check --all-targets clean · cargo test 0 failed · the headless run',
  'still exits 0 · python3 tools/norms.py clean · your picture taken AND looked at ·',
  'at least one of your tests seen going red, with the output in your report.',
].join('\n'), { label: 'gear:' + g.label, phase: 'Gear' })))

log('Gear built — rope next')

phase('Rope')

const ROPE = [
  {
    id: 'F-004', label: 'pendulum', file: 'src/vector/rope.rs',
    task: [
      'F-004 PENDULUM PHYSICS with two hooks — "two set hooks produce real centripetal motion',
      'via a rope constraint instead of linear pulling. Basis for momentum chaining."',
      'Acceptance: "the player visibly describes an arc; speed increases when swinging out".',
      '',
      'THE SOLUTION IS MEASURED (see the architecture block): one avian `DistanceJoint` per',
      'anchored hook, `limits = (0, L)`. L is the distance at the moment of anchoring, capped',
      'at `hook_range_m` — and QUESTIONS.md Q-013 records that no source ever defined a maximum',
      'rope length, so this is an assumption you inherit, not one you invent.',
      'Two taut ropes work: measured 0.41 mm sag in the normal case and 0.23 mm constraint',
      'error in the degenerate one (both anchors nearly collinear).',
      '',
      'CRITERIA — the weak ones are forbidden, they were already refuted:',
      '  NOT "speed at the bottom > speed at the turning point" — a free fall passes that.',
      '  Instead: the HORIZONTAL velocity component at the low point against `sqrt(2*g*h)`',
      '  within 2 % (a free fall has exactly zero there), AND `| |pos-anchor| - L | <= 1e-3`',
      '  held over the whole swing, AND the path length divided by the chord > 1.3 (a straight',
      '  pull cannot produce that).',
      '  Plus the two-hook case explicitly, including the degenerate one.',
    ].join('\n'),
  },
  {
    id: 'F-005', label: 'reel', file: 'src/vector/reel.rs',
    task: [
      'F-005 REEL-IN — "controlled reduction of rope length for height gain. Manually',
      'controlled, consumes gas." Acceptance: "the player can gain height from the low point;',
      'gas consumption is noticeable."',
      '',
      'THE MECHANISM IS MEASURED AND IT IS THE HEART OF THE GAME: shortening `limits.max`',
      'preserves angular momentum — v0 20 m/s becomes 58.23 m/s = exactly 20 * 8.7/3. That',
      'acceleration IS the ODM feeling. A hand-written clamp gives exactly 20.000 and kills it.',
      '',
      'TWO THINGS ARE NOT NEGOTIABLE, both measured:',
      '  1. Shorten **per substep**, never per tick. Per tick injects `rate x SubstepCount`',
      '     = 677 m/s and throws the player 24 m through a wall. Per substep: 130 m/s.',
      '     avian has a `SubstepSchedule` — find out how to run in it (source, not guessing).',
      '  2. `MaxLinearSpeed` must be on the player (the seam agent set it). Without it, reeling',
      '     reaches 116-130 m/s at a limit of 75. With it: 75.44 m/s.',
      'Gas comes from the F-018 booking component. `rope_pull_m_s` and `rope_min_m` are in RON.',
      '',
      'CRITERIA: reeling from 20 m to 5 m increases speed by a factor near the angular-momentum',
      'prediction (goes red if someone reels by teleporting the position) · the rope never gets',
      'shorter than `rope_min_m` · with an empty tank nothing happens at all · and the one that',
      'matters: reeling toward an anchor BEHIND a 0.5 m wall does not put the player through it',
      '(measured: -0.0043 m worst case with the clamp and per-substep shortening).',
    ].join('\n'),
  },
]

const rope = await parallel(ROPE.map((r) => () => agent([
  BASE, '',
  '=== YOUR COMMISSION: ' + r.id + ' — ' + r.label.toUpperCase() + ' ===',
  '',
  'YOU OWN: ' + W + '/' + r.file + ' and ' + W + '/tests/vector_' + r.label + '.rs (new)',
  'and ' + W + '/scripts/' + r.id.toLowerCase() + '-' + r.label + '.txt (new)',
  'and ' + W + '/docs/images/' + r.id.toLowerCase() + '-' + r.label + '.png (new).',
  'YOU DO NOT TOUCH anything else. One other agent is in src/vector/ right now.',
  '',
  r.task,
  '',
  'WHAT WAS BUILT BEFORE YOU — the seam and the four gear commissions. Read them, your code',
  'sits on top and must use their components, not rebuild them:',
  '--- SEAM ---', seam,
  '--- HOOKS (F-001) ---', gear[0],
  '--- AIMING (F-002) ---', gear[1],
  '--- GAS (F-018) ---', gear[2],
  '--- BOOST (F-007) ---', gear[3],
  '',
  'ACCEPTANCE: cargo check --all-targets clean · cargo test 0 failed · the headless run',
  'still exits 0 · python3 tools/norms.py clean · your picture taken AND looked at ·',
  'at least one test seen going red, output in the report.',
].join('\n'), { label: 'rope:' + r.label, phase: 'Rope' })))

log('Vector Gear built — now the attack')

phase('Proof')

const PROOF = [
  BASE, '',
  '=== YOUR COMMISSION: ATTACK ALL OF IT, THEN JUDGE THE STAGES ===',
  '',
  'You change nothing in the project. ' + S + '/stage3probe/ is yours.',
  '',
  'Seven agents just built the Vector Gear. Here are their reports:',
  '--- SEAM ---', seam,
  '--- F-001 HOOKS ---', gear[0],
  '--- F-002 AIMING ---', gear[1],
  '--- F-018 GAS ---', gear[2],
  '--- F-007 BOOST ---', gear[3],
  '--- F-004 PENDULUM ---', rope[0],
  '--- F-005 REEL-IN ---', rope[1],
  '',
  '1. **Run the acceptance yourself.** cargo check --all-targets · cargo test (count them,',
  '   a silently lost test is the worst outcome) · the headless run · python3 tools/norms.py.',
  '   Five agents green on their own are not automatically green together.',
  '2. **Look at every picture in ' + W + '/docs/images/** and describe independently what you',
  '   see. Does the hook picture show a hook? Is a rope visible and taut? Or is it a city with',
  '   nothing happening in it? A picture that does not show the claim is not evidence.',
  '3. **For EVERY criterion: construct a broken implementation that passes it green.** Where',
  '   you succeed, the criterion is worthless — say how to sharpen it. Be especially',
  '   suspicious of anything that merely proves avian works: that says nothing about OUR feature.',
  '4. **One writer per field.** grep for who writes `LinearVelocity`, `Position`, `Transform`,',
  '   `Gas`, the hook state. avian counts as a writer. Any second writer is a finding.',
  '5. **Play it.** Write a script that actually flies: aim at a roof, hook, swing, reel in,',
  '   boost, release. Run it with --offscreen and take pictures. Does the player move like',
  '   the Vector Gear, or does he hang in the air? Measure the speed over the run and say',
  '   whether this looks like a game.',
  '6. **Judge each F-ID: ⬜, 🟨 or 🟧** — with the reason. 🟧 needs a picture that shows it,',
  '   a measured number, and a test seen going red. Be strict: the supervisor writes your',
  '   judgement into docs/features.ron, and ✅ is the user\'s alone.',
  '',
  'Close with the three things that would most embarrass this project if the user tried it now.',
].join('\n')

const proof = await agent(PROOF, { label: 'stage3:proof', phase: 'Proof' })

return { seam: seam, gear: gear, rope: rope, proof: proof }
